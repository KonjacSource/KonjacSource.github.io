<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>如何形式化验证一场技术面试</title>
    <link href="/2024/06/24/%E5%A6%82%E4%BD%95%E5%BD%A2%E5%BC%8F%E5%8C%96%E9%AA%8C%E8%AF%81%E4%B8%80%E5%9C%BA%E6%8A%80%E6%9C%AF%E9%9D%A2%E8%AF%95/"/>
    <url>/2024/06/24/%E5%A6%82%E4%BD%95%E5%BD%A2%E5%BC%8F%E5%8C%96%E9%AA%8C%E8%AF%81%E4%B8%80%E5%9C%BA%E6%8A%80%E6%9C%AF%E9%9D%A2%E8%AF%95/</url>
    
    <content type="html"><![CDATA[<p>完整代码可见<a href="https://github.com/KonjacSource/NQueens"class="uri">https://github.com/KonjacSource/NQueens</a>.</p><p>标题捏自<ahref="https://aphyr.com/posts/342-typing-the-technical-interview"class="uri">https://aphyr.com/posts/342-typing-the-technical-interview</a>,这里有一篇中文翻译<a href="https://zhuanlan.zhihu.com/p/84634204"class="uri">https://zhuanlan.zhihu.com/p/84634204</a>, 不会写小作文,所以只偷个名字😢.</p><p>本文意图展示一下如何确保正确性地编写一个程序, 我们会使用Agda,在编写代码的同时证明代码的正确性.</p><p>题目是N皇后问题, 即, 如何在一个 n×n 的国际象棋棋盘上布置 n个皇后使她们不能互相攻击.</p><p>我们建立一个模块(还是借用Haskell的高亮😢)</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-keyword">module</span> NQueens (<span class="hljs-title">n</span> : ℕ) <span class="hljs-keyword">where</span> <br></code></pre></td></tr></table></figure><p>Agda允许模块具有参数, 这里参数 n 就代表棋盘的规模,当其他人导入这个模块的时候就需要提供一个具体值.</p><p>我们可以用两个数表示一个棋子的位置</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">Pos</span> : <span class="hljs-type">Set</span> <br><span class="hljs-type">Pos</span> = <span class="hljs-type">Fin</span> n × <span class="hljs-type">Fin</span> n<br></code></pre></td></tr></table></figure><p>其中 <code>Fin n</code> 是一个具有 <code>n</code> 个元素的类型,可以用它来表示一个大于等于 0, 小于 n 的自然数, 每个 <code>Pos</code>由两个 <code>Fin n</code> 构成, 用来表示横纵坐标.</p><p>所以放置了 <code>m</code> 个棋子的棋盘的类型就是</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">Board</span> : ℕ → <span class="hljs-type">Set</span> <br><span class="hljs-type">Board</span> m = <span class="hljs-type">Vec</span> <span class="hljs-type">Pos</span> m<br></code></pre></td></tr></table></figure><p>现在我们考虑如何表达一个棋盘是安全的.</p><p>两个棋子是否在同行?</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">row</span> : <span class="hljs-type">Pos</span> → <span class="hljs-type">Pos</span> → <span class="hljs-type">Set</span> <br><span class="hljs-title">row</span> (x₁ , y₁) (x₂ , y₂) = y₁ ≡ y₂<br></code></pre></td></tr></table></figure><p>同列?</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">col</span> : <span class="hljs-type">Pos</span> → <span class="hljs-type">Pos</span> → <span class="hljs-type">Set</span> <br><span class="hljs-title">col</span> (x₁ , y₁) (x₂ , y₂) = x₁ ≡ x₂<br></code></pre></td></tr></table></figure><p>同斜线?</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">raise</span> : ∀ &#123;m&#125; → <span class="hljs-type">Fin</span> m → <span class="hljs-type">Fin</span> (suc m)<br><span class="hljs-title">raise</span> zero = zero <br><span class="hljs-title">raise</span> (suc x) = suc (raise x)<br><br>∣_-_∣ : ∀ &#123;m&#125; → <span class="hljs-type">Fin</span> m → <span class="hljs-type">Fin</span> m → <span class="hljs-type">Fin</span> m<br>∣ zero  - zero  ∣ = zero<br>∣ zero  - suc n ∣ = suc n<br>∣ suc m - zero  ∣ = suc m<br>∣ suc m - suc n ∣ = raise ∣ m - n ∣<br><br><span class="hljs-title">slash</span> : <span class="hljs-type">Pos</span> → <span class="hljs-type">Pos</span> → <span class="hljs-type">Set</span> <br><span class="hljs-title">slash</span> (x , y) (x′ , y′) = ∣ x - x′ ∣ ≡ ∣ y - y′ ∣ <br></code></pre></td></tr></table></figure><p>同斜线就是判断两个棋子横纵坐标分别相减之后的绝对值是否相等.</p><p>把上面这些命题组合, 我们就得到了两个棋子不能互相吃到的命题,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">safe</span> : <span class="hljs-type">Pos</span> → <span class="hljs-type">Pos</span> → <span class="hljs-type">Set</span> <br><span class="hljs-title">safe</span> p q = ¬ (row p q) <br>             × ¬ (col p q) <br>             × ¬ (slash p q) <br>           ⊎ p ≡ q <span class="hljs-comment">-- 如果两棋子位置相同, 那么它们就是同一个棋子, 所以也应当被认为是安全的.</span><br></code></pre></td></tr></table></figure><p>我们现在需要用到一个十分好用的类型 <code>Dec</code>,它的定义如下,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Dec</span> (<span class="hljs-type">A</span> : <span class="hljs-type">Set</span>) : <span class="hljs-type">Set</span> where</span><br>  yes :   <span class="hljs-type">A</span> → <span class="hljs-type">Dec</span> <span class="hljs-type">A</span><br>  no  : ¬ <span class="hljs-type">A</span> → <span class="hljs-type">Dec</span> <span class="hljs-type">A</span><br></code></pre></td></tr></table></figure><p><code>Dec A</code> 意味着 <code>A</code> 是一个可判定的命题, 一个<code>Dec A</code> 的元素不仅要机械得判断 <code>A</code> 是否成立,还要给出 <code>A</code>成立或不成立的理由(就是一个<code>A</code>或<code>¬ A</code>的实例), 关于<code>Dec</code> 更详细的介绍参见<ahref="https://agda-zh.github.io/PLFA-zh/Decidable/"class="uri">https://agda-zh.github.io/PLFA-zh/Decidable/</a>.</p><p>现在我们要说, 对于任何两个位置 <code>p q : Pos</code>,<code>safe p q</code> 是可判定的,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><br><br><br><span class="hljs-comment">-- 判定两个坐标是否相等</span><br><span class="hljs-title">_</span>≟ᵖ_ : (p q : <span class="hljs-type">Pos</span>) → (<span class="hljs-type">Dec</span> (p ≡ q))<br><span class="hljs-title">_</span>≟ᵖ_ = ≟-× _≟_ _≟_ <br>  <span class="hljs-keyword">where</span> <br>    ≟-× : ∀ &#123;ℓ ℓ′&#125; &#123;<span class="hljs-type">A</span> : <span class="hljs-type">Set</span> ℓ&#125; &#123;<span class="hljs-type">B</span> : <span class="hljs-type">Set</span> ℓ′&#125;<br>        → (eqA : (a b : <span class="hljs-type">A</span>) → <span class="hljs-type">Dec</span> (a ≡ b)) → (eqB : (a b : <span class="hljs-type">B</span>) → <span class="hljs-type">Dec</span> (a ≡ b)) <br>        → (x y : <span class="hljs-type">A</span> × <span class="hljs-type">B</span>)<br>        → <span class="hljs-type">Dec</span> (x ≡ y)<br>    ≟-× eqA eqB (xa , xb) (ya , yb) <br>        with eqA xa ya<br>    ...    | no ¬a = no (λ &#123;refl → ¬a refl&#125;)<br>    ...    | yes a <br>          with eqB xb yb <br>    ...      | no ¬b = no (λ &#123;refl → ¬b refl&#125;)<br>    ...      | yes b = yes<br>                      (subst (λ s → (xa , xb) ≡ (s , yb)) a <br>                    $ (subst (λ s → (xa , xb) ≡ (xa , s)) b refl))             <br><span class="hljs-comment">-- 判定是否同行</span><br><span class="hljs-title">row</span>? : (p q : <span class="hljs-type">Pos</span>) → <span class="hljs-type">Dec</span> (row p q)<br><span class="hljs-title">row</span>? (x₁ , y₁) (x₂ , y₂) = y₁ ≟ y₂<br><span class="hljs-comment">-- 判定是否同列</span><br><span class="hljs-title">col</span>? : (p q : <span class="hljs-type">Pos</span>) → <span class="hljs-type">Dec</span> (col p q)<br><span class="hljs-title">col</span>? (x₁ , y₁) (x₂ , y₂) = x₁ ≟ x₂<br><span class="hljs-comment">-- 判定是否同斜线</span><br><span class="hljs-title">slash</span>? : (p q : <span class="hljs-type">Pos</span>) → <span class="hljs-type">Dec</span> (slash p q)<br><span class="hljs-title">slash</span>? (x , y) (x′ , y′) = ∣ x -ᶠ x′ ∣ ≟ ∣ y -ᶠ y′ ∣ <br><span class="hljs-comment">-- 判定是否安全</span><br><span class="hljs-title">safe</span>? : (p q : <span class="hljs-type">Pos</span>) → <span class="hljs-type">Dec</span> (<span class="hljs-keyword">safe</span> p q)<br><span class="hljs-title">safe</span>? p q =     ¬? (row? p q)<br>          ×-dec ¬? (col? p q)<br>          ×-dec ¬? (slash? p q)<br>          ⊎-dec (p ≟ᵖ q)<br></code></pre></td></tr></table></figure><p>现在我们可以来判断一个棋盘是否安全了,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">allCombine</span> : (n : ℕ) → <span class="hljs-type">Vec</span> (<span class="hljs-type">Fin</span> n × <span class="hljs-type">Fin</span> n) (n * n) <br><span class="hljs-title">allCombine</span> n = <br>  <span class="hljs-type">Vec</span>.concat $ <br>    <span class="hljs-type">Vec</span>.map (λ f → <span class="hljs-type">Vec</span>.map (_, f) (<span class="hljs-type">Vec</span>.allFin n)) (<span class="hljs-type">Vec</span>.allFin n) <br><br><span class="hljs-comment">-- 从棋盘上选两棋子做组合, 返回所有的取法</span><br><span class="hljs-title">eachSafe</span> : ∀ &#123;n&#125; → <span class="hljs-type">Board</span> n → <span class="hljs-type">Vec</span> (Σ <span class="hljs-type">Set</span> <span class="hljs-type">Dec</span>) (n * n)<br><span class="hljs-title">eachSafe</span> b = <span class="hljs-type">Vec</span>.map <br>  (λ &#123;(i , j) → <br>    ( <span class="hljs-keyword">safe</span>  (lookup b i) (lookup b j) <br>    , <span class="hljs-keyword">safe</span>? (lookup b i) (lookup b j))&#125;) <br>  (allCombine _)<br><br><span class="hljs-comment">-- 对一个列表里的命题做合取</span><br><span class="hljs-title">allProp</span> : ∀ &#123;n&#125; → <span class="hljs-type">Vec</span> <span class="hljs-type">Set</span> n → <span class="hljs-type">Set</span><br><span class="hljs-title">allProp</span> [] = ⊤<br><span class="hljs-title">allProp</span> (x ∷ xs) = x × allProp xs<br><br><span class="hljs-comment">-- 整个棋盘是否安全?</span><br><span class="hljs-type">AllSafe</span> : ∀ &#123;n&#125; → <span class="hljs-type">Board</span> n → <span class="hljs-type">Set</span> <br><span class="hljs-type">AllSafe</span> &#123;n&#125; b = allProp (<span class="hljs-type">Vec</span>.map proj₁ (eachSafe b))<br></code></pre></td></tr></table></figure><p><code>AllSafe</code> 当然也是可判定的,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">allProp</span>? : ∀ &#123;n&#125; <br>          → (vec : <span class="hljs-type">Vec</span> (Σ <span class="hljs-type">Set</span> <span class="hljs-type">Dec</span>) n) <br>          → <span class="hljs-type">Dec</span> (allProp (<span class="hljs-type">Vec</span>.map proj₁ vec))<br><span class="hljs-title">allProp</span>? [] = yes tt <br><span class="hljs-title">allProp</span>? ((x , xd) ∷ xs) = xd ×-dec allProp? xs<br><br><span class="hljs-type">AllSafe</span>? : &#123;n : ℕ&#125; → (b : <span class="hljs-type">Board</span> n) → <span class="hljs-type">Dec</span> (<span class="hljs-type">AllSafe</span> b)<br><span class="hljs-type">AllSafe</span>? &#123;n&#125; b = allProp? (eachSafe b)<br></code></pre></td></tr></table></figure><p>如果一个棋盘是一个解, 那它不仅要满足<code>AllSafe</code>,还要有恰好<code>n</code>个棋子在上面,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">Solution</span> : <span class="hljs-type">Board</span> n → <span class="hljs-type">Set</span> <br><span class="hljs-type">Solution</span> = <span class="hljs-type">AllSafe</span><br></code></pre></td></tr></table></figure><p>我们搜索答案的策略是一行一行地放置棋子,我们保证在搜索过程棋子要保证顺序. 所以我们再来定义一个谓词,用来检定一个棋盘是否是有序的,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">OrderBoard</span> : ∀ &#123;m&#125; → <span class="hljs-type">Board</span> m → <span class="hljs-type">Set</span> <br><span class="hljs-type">OrderBoard</span> &#123;m&#125; [] = ⊤ <br><span class="hljs-type">OrderBoard</span> &#123;m&#125; ((_ , row₁) ∷ rest) = suc (toℕ row₁) ≡ m × <span class="hljs-type">OrderBoard</span> rest<br><br><span class="hljs-type">OrderBoard</span>? : ∀ &#123;m&#125; → (b : <span class="hljs-type">Board</span> m) → <span class="hljs-type">Dec</span> (<span class="hljs-type">OrderBoard</span> b)<br><span class="hljs-type">OrderBoard</span>? &#123;m&#125; [] = yes tt<br><span class="hljs-type">OrderBoard</span>? &#123;m&#125; ((_ , row₁) ∷ rest) <br>  with suc (toℕ row₁) ≟ⁿ m | <span class="hljs-type">OrderBoard</span>? rest <br>...  | yes a               | yes b = yes (a , b)<br>...  | _                   | no ¬b = no  (¬b ∘ proj₂)<br>...  | no ¬a               | _     = no  (¬a ∘ proj₁)<br><br><span class="hljs-type">OrderSafe</span> : ∀ &#123;m&#125; → <span class="hljs-type">Board</span> m → <span class="hljs-type">Set</span> <br><span class="hljs-type">OrderSafe</span> board = <span class="hljs-type">AllSafe</span> board × <span class="hljs-type">OrderBoard</span> board<br><br><span class="hljs-type">OrderSafe</span>? : ∀ &#123;m&#125; → (b : <span class="hljs-type">Board</span> m) → <span class="hljs-type">Dec</span> (<span class="hljs-type">OrderSafe</span> b)<br><span class="hljs-type">OrderSafe</span>? board = <span class="hljs-type">AllSafe</span>? board ×-dec <span class="hljs-type">OrderBoard</span>? board<br></code></pre></td></tr></table></figure><p>然后我们再来定义一个<code>Dec</code>版本的<code>filter</code>,它接受一个可判定的谓词<code>f</code>,经他过滤之后的列表的每个元素都自动具有<code>f</code>要求的性质,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">decFilter</span> : ∀ &#123;m&#125; &#123;<span class="hljs-type">A</span> : <span class="hljs-type">Set</span>&#125; <br>          → (f : <span class="hljs-type">A</span> → <span class="hljs-type">Set</span>) <br>          → (∀ a → <span class="hljs-type">Dec</span> (f a)) <br>          → <span class="hljs-type">Vec</span> <span class="hljs-type">A</span> m → <span class="hljs-type">List</span> (Σ <span class="hljs-type">A</span> f) <br><span class="hljs-title">decFilter</span> f f-dec [] = [] <br><span class="hljs-title">decFilter</span> f f-dec (x ∷ xs) <br>  with f-dec x <br>...  | yes a = (x , a) ∷ decFilter f f-dec xs<br>...  | no ¬a = decFilter f f-dec xs<br></code></pre></td></tr></table></figure><p>现在我们终于可以来搜索了,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">putNext</span> : &#123;m : ℕ&#125; <br>        → m &lt; n<br>        →      (Σ (<span class="hljs-type">Board</span>      m ) <span class="hljs-type">OrderSafe</span>) <br>        → <span class="hljs-type">List</span> (Σ (<span class="hljs-type">Board</span> (suc m)) <span class="hljs-type">OrderSafe</span>)<br><span class="hljs-title">putNext</span> &#123;m&#125; h (board , <span class="hljs-keyword">safe</span> , order) = decFilter <span class="hljs-type">OrderSafe</span> <span class="hljs-type">OrderSafe</span>? newBoard<br>  <span class="hljs-keyword">where</span> <br>    newBoard : <span class="hljs-type">Vec</span> (<span class="hljs-type">Board</span> (suc m)) n<br>    newBoard = <span class="hljs-type">Vec</span>.map <br>        (λ x → (x , fromℕ&lt; h) ∷ board)<br>        (<span class="hljs-type">Vec</span>.allFin n)<br></code></pre></td></tr></table></figure><p>这个函数接收一个棋盘, 然后返回所有安全的下一个棋子布置.</p><p>反复迭代我们就可以得到一个完整的搜索函数,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">solve</span> : ∀ m → suc m ≤ n → <span class="hljs-type">List</span> (Σ (<span class="hljs-type">Board</span> (suc m)) <span class="hljs-type">OrderSafe</span>)<br><span class="hljs-title">solve</span> zero (s≤s h) = putNext &#123;zero&#125; (s≤s z≤n) ([] , tt , tt)<br><span class="hljs-title">solve</span> (suc m) (s≤s h) = <span class="hljs-type">List</span>.concatMap (putNext (s≤s h)) (solve m (ℕₚ.m≤n⇒m≤<span class="hljs-number">1</span>+n h))<br></code></pre></td></tr></table></figure><p>然后执行这个函数,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">solutions</span>′ : <span class="hljs-type">List</span> (Σ (<span class="hljs-type">Board</span> n) <span class="hljs-type">AllSafe</span>)<br><span class="hljs-title">solutions</span>′ = <span class="hljs-keyword">case</span> inspect n <span class="hljs-keyword">of</span> λ <span class="hljs-keyword">where</span> <br>  (zero with≡ _) → []<br>  (suc n′ with≡ h) → subst (λ n → <span class="hljs-type">List</span> (Σ (<span class="hljs-type">Board</span> n) <span class="hljs-type">AllSafe</span>)) (sym h) <br>    (<span class="hljs-type">List</span>.map (λ &#123;(b , s , _) → (b , s)&#125;) <br>              (solve n′ (subst (_≤ n) h ℕₚ.≤-refl)))<br><br><span class="hljs-title">solutions</span> : <span class="hljs-type">List</span> (Σ (<span class="hljs-type">Board</span> n) <span class="hljs-type">Solution</span>)             <br><span class="hljs-title">solutions</span> = solutions′ <br></code></pre></td></tr></table></figure><p><code>solutions</code>就是N皇后问题所有的解,注意这里列表的元素不仅包含了棋盘, 还包含了这个棋盘是解的证明,这就是正确性的体现.</p>]]></content>
    
    
    
    <tags>
      
      <tag>编程</tag>
      
      <tag>计算机</tag>
      
      <tag>Agda</tag>
      
      <tag>形式验证</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>如何浪费青春</title>
    <link href="/2024/06/11/%E6%B5%AA%E8%B4%B9%E9%9D%92%E6%98%A5/"/>
    <url>/2024/06/11/%E6%B5%AA%E8%B4%B9%E9%9D%92%E6%98%A5/</url>
    
    <content type="html"><![CDATA[<center><b> <i> 社交和酒精都有成瘾性 </i> </b></center><p align="right">-- 我说的</p><hr /><p>2023年秋末的一个夜晚, 我在学校附近散步, 路遇酒吧一条街.</p><p>请求酒鬼同学带我去,</p><p>去了.</p><p>浪费时间了,</p><p>遇到一个女人了, 她看上我了,</p><p>我觉得她有点胖, 并且不太好看,</p><p>但我喝多了, 她拉我去开房,</p><p>我虽然有些不情愿, 但没有拒绝,</p><p>那天晚上我在床上确诊阳痿了, 一夜过后, 处身依旧.</p><p>次日她走了之后就不再鸟我了.</p><p>女人, 很奇妙吧.</p><hr /><p>寒假过后, 冬末春初.</p><p>发现学校附近的一个酒吧C, 酒很好喝,这里成了我每周都来三五回的地方.</p><p>好喝得很, 好喝死了.</p><hr /><p>某日彻夜喝完之后,</p><p>在充满酒蒙子的早餐店, 隔壁有一桌人,</p><p>有一个亚逼女孩, 她盯着我看, 看了好几眼, 对视之后她问我是不是高中生😃.我说不是, 我是XX大学的, 她也是, 她是我学妹, 她叫小M.</p><p>恰了v, 约了之后去她常去的地方喝喝. 那里是一个兼唱片店的酒吧D,地方很小, 目测四十多平, 去那里喝的所有人都会聊到一块去.</p><p>她搞乐队, 叫我去看他们演出, 过去了, 听了, 转场了, 去了那个唱片店,她也去了, 喝的很多, 我和她在门口赛跑, 我摔倒了, 腿摔破了.</p><p>在屋里, 座满了, 我们席地而坐, 我躺在她盘坐的腿上, 她揉我头发.</p><p>在一屋子人的起哄下我们接吻, 亲完之后她说我: "其实我是 bisexual" .然后转头去亲旁边的女孩子.</p><p>过后确认她完全没有想要交往的意愿, 但还是亲我.</p><p>女人, 很奇妙吧.</p><hr /><p>2024年4月19日</p><p>我的酒鬼舍友告诉我他在离学校较远的一个区域发现了一个非常随性的酒吧H,说那里的调酒师很随性, 会直接说: "我不想做了, 你自己进来做吧".很多酒都不会调, 点哪个缺哪个, 我的朋友当场教她做B52.生意很差(我写下这些文字的时候那个酒吧已经暂停营业了),在我朋友离开那个酒吧后, 那个调酒师直接打烊下班了, 彼时才十点不到.他说那里有只猫, 很粘人.</p><p>他说, 学校附近的酒吧都逛够了, 我们去那边探索新店吧.那个调酒姐甚至向他推荐了那边的一个标准模型酒吧T.</p><p>我觉得这很好, 便约好过两日去 H 和 T 看看.</p><p><strong>T.B.C.</strong> <!-- 4月21日我的酒鬼舍友告诉我他在酒吧H, 叫我速来, 并带一份烤冷面. 我过去了之后和他坐在了吧台.  --></p>]]></content>
    
    
    
    <tags>
      
      <tag>杂谈</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>写给一般计算机人的Lean4安利</title>
    <link href="/2023/11/25/Lean4%E5%AE%89%E5%88%A9/"/>
    <url>/2023/11/25/Lean4%E5%AE%89%E5%88%A9/</url>
    
    <content type="html"><![CDATA[<p>本文主要给有计算机背景(起码懂一门强类型编程语言,如果读者懂函数式编程或者 Rust 就更好了)的人介绍一下 Lean4(作为一个编程语言而不是证明助手).</p><p>环境配置不在本文介绍范围内, 请参考<ahref="https://lean-lang.org/lean4/doc/setup.html"class="uri">https://lean-lang.org/lean4/doc/setup.html</a>或者参考这里<ahref="https://zhuanlan.zhihu.com/p/656770107"class="uri">https://zhuanlan.zhihu.com/p/656770107</a>.</p><p><strong>另外由于 Markdown 缺少 Lean 的高亮, 我们会借用 Haskell的高亮</strong>, 文中的代码除特殊注明外全部为 Lean4代码</p><h2 id="lean4-是什么">Lean4 是什么</h2><p>Lean 是微软研究院推出的一款定理证明器,它同时也是一款极其先进的通用编程语言, 如今已到第四个大版本.在此文中我们忽略 Lean 在前沿数学中的突出贡献, 而只介绍作为编程语言的Lean4.</p><h2 id="lean4-是一个函数式语言">Lean4 是一个函数式语言</h2><p>Lean4 程序由一系列定义构成, 最顶层的定义可以为函数或者是类型,我们这里先介绍函数, 如下是一个简单的函数定义(这里借用了 Haskell的高亮):</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> addOne(x : <span class="hljs-type">Int</span>) : <span class="hljs-type">Int</span> := x + <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>这里用到的语法几乎是显然的, 我们定义了一个函数 <code>addOne</code>,它接受一个类型为 <code>Int</code> 的变量 <code>x</code>,返回一个<code>Int</code>, 返回值类型可以省略交由 Lean 来推导</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> addOne(x : <span class="hljs-type">Int</span>) := x + <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>可以在 vscode 里快速测试这个函数(如果你配置好了Lean4的环境的话)</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-meta">#eval addOne 2</span><br></code></pre></td></tr></table></figure><p>当光标放在这一行的时候, Lean Infoview 窗口会显示 <code>3</code>.这里调用函数直接使用空格分割函数和实参, 而不像一般语言中常见的<code>addOne(2)</code>, 如果您不了解函数式编程的话,我们马上能看出为什么要如此设计.</p><h3 id="多参数函数">多参数函数</h3><p>在一些常见的编程语言中, 我们可能会如此定义一个多参函数:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 这是 Python 代码</span><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">add1</span>(<span class="hljs-params">x : Int , y : Int</span>):<br>  <span class="hljs-keyword">return</span> x + y<br></code></pre></td></tr></table></figure><p>这可以看作是一个从两个整数到一个整数的映射, 我们记作<code>Int × Int → Int</code>. 给它两个整数<code>x y</code>,它才还你一个整数<code>x + y</code>. 描述这个函数还有另一个思路:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 这是 Python 代码</span><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">add2</span>(<span class="hljs-params">x : Int</span>):<br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">lambda</span> y : x + y<br></code></pre></td></tr></table></figure><p>对于这个函数, 如果你只给它一个整数<code>x</code>, 它会变成另一个函数,这个函数会要求你再给一个整数<code>y</code>,然后才还你一个整数<code>x+y</code>.这样这个函数可以看作一个从整数到一个函数的映射, 我们记作<code>Int → (Int → Int)</code>, 或者省略括号直接记作<code>Int → Int → Int</code>. 我们默认给函数箭头加括号的规则是从右往左,注意这个箭头<code>→</code>并不满足结合律, 即,<code>Int → (Int → Int)</code> 和 <code>(Int → Int) → Int</code>显然不是一回事.</p><p>相比于大多数人熟悉的第一种写法,Lean(和一大票函数式语言)更倾向于第二种写法, 这样我们可以写一个 Lean4版本的 <code>add</code> 函数.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> add (x : <span class="hljs-type">Int</span>) (y : <span class="hljs-type">Int</span>) := x + y<br></code></pre></td></tr></table></figure><p>如果两个(或多个)参数的类型一样, 它们可以被合并写作</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> add (x y : <span class="hljs-type">Int</span>) := x + y<br></code></pre></td></tr></table></figure><p>使用 <code>#eval add 2 3</code> 可以测试一下这个函数, 使用<code>#check add</code> 可以看一看这个函数的类型.然后我们可以更简单地定义 <code>addOne</code> 函数</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> addOne := add <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><h3 id="lambda-表达式">lambda 表达式</h3><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> add := fun (x y : <span class="hljs-type">Int</span>) =&gt; x + y<br></code></pre></td></tr></table></figure><p>或者在上下文类型明确时不去标注参数类型</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> add : <span class="hljs-type">Int</span> → <span class="hljs-type">Int</span> → <span class="hljs-type">Int</span> := fun x y =&gt; x + y<br></code></pre></td></tr></table></figure><p>在 vscode 中依次按下 空格] 可以打出符号<code>→</code>,如果不喜欢unicode, 也可以使用 <code>-&gt;</code> 代替.</p><h2 id="lean4-是一个强类型语言">Lean4 是一个强类型语言</h2><p>强类型语言意味着这个语言中的所有表达式都有一个所属的类型,并且这些类型会在编译期确定并检查, 有效的类型检查是程序安全性的重要保障,强大的类型系统是语言抽象能力的重要体现.</p><p>我们已经见识到了两种类型, 一个整数类型 <code>Int</code>一个是函数类型 <code>→</code>, 后者其实是我们常说的泛型,这点我们稍后再谈.</p><h3 id="一些常用类型">一些常用类型</h3><p>我们列举出一些常用的类型和它们对应的值(<code>--</code> 是 Lean的行注释的起始符)</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-number">3</span>  : <span class="hljs-type">Int</span> <span class="hljs-comment">-- 整数</span><br>-<span class="hljs-number">2</span> : <span class="hljs-type">Int</span> <br><br><span class="hljs-number">0</span> : <span class="hljs-type">Nat</span> <span class="hljs-comment">-- 自然数</span><br><span class="hljs-number">1</span> : <span class="hljs-type">Nat</span><br><span class="hljs-number">2</span> : <span class="hljs-type">Nat</span><br><br><span class="hljs-number">3.14</span> : <span class="hljs-type">Float</span> <span class="hljs-comment">-- 浮点数</span><br><span class="hljs-number">3.00</span> : <span class="hljs-type">Float</span><br><br><span class="hljs-string">&#x27;c&#x27;</span> : <span class="hljs-type">Char</span> <span class="hljs-comment">-- 字符</span><br><br><span class="hljs-string">&quot;Hello&quot;</span> : <span class="hljs-type">String</span> <span class="hljs-comment">-- 字符串</span><br><br><span class="hljs-title">true</span> : <span class="hljs-type">Bool</span> <span class="hljs-comment">-- 布尔</span><br><span class="hljs-title">false</span> : <span class="hljs-type">Bool</span> <br><br>() : <span class="hljs-type">Unit</span> <span class="hljs-comment">-- 单元类型, 这个类型只有一个元素 ()</span><br><br><span class="hljs-title">fun</span> (x : <span class="hljs-type">Int</span>) =&gt; x + <span class="hljs-number">1</span> : <span class="hljs-type">Int</span> → <span class="hljs-type">Int</span><br></code></pre></td></tr></table></figure><h3 id="定义类型">定义类型</h3><p>我们可以用如下语法定义一个结构体</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">structure</span> <span class="hljs-type">Point2D</span> <span class="hljs-keyword">where</span><br>  x : <span class="hljs-type">Float</span><br>  y : <span class="hljs-type">Float</span><br><span class="hljs-title">deriving</span> <span class="hljs-type">Repr</span><br></code></pre></td></tr></table></figure><p><code>Pointe2D</code> 是我们定义的新类型的名字, <code>x y</code>是两个分量的名字, 后面的<code>: Float</code>说明了分量的类型,如果你熟悉Rust, 那么最后一行的作用就是<code>#[derive(Display)]</code>的作用; 如果你熟悉 <code>Haskell</code> ,那么最后一行的作用与 <code>deriving Show</code> 无异.那些不熟悉这些语言的读者也不必担心, 这句话的作用就是让 Lean帮我们生成一个可以输出 <code>Point2D</code> 的函数.这样我们就能<code>#eval</code> 一个类型为 <code>Point2D</code> 的表达式.</p><p>我们可以使用 <code>Point2D.mk</code> 函数构造一个<code>Point2D</code></p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> origin : <span class="hljs-type">Point2D</span> := <span class="hljs-type">Point2D</span>.mk <span class="hljs-number">0.0</span> <span class="hljs-number">0.0</span><br></code></pre></td></tr></table></figure><p><code>Point2D.mk</code> 是一个由 Lean 自动生成的函数, 称为构造子,用来构造一个值. 在上下文类型明确的情况下, 也可以使用如下语法构造<code>Point2D</code></p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> origin : <span class="hljs-type">Point2D</span> := &#123;x := <span class="hljs-number">0.0</span>, y := <span class="hljs-number">0.0</span>&#125;<br></code></pre></td></tr></table></figure><p>如果我们想给构造子命名, 我们可以这样定义 <code>Point2D</code></p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">structure</span> <span class="hljs-type">Point2D</span> <span class="hljs-keyword">where</span><br>  point :: <br>  x : <span class="hljs-type">Float</span><br>  y : <span class="hljs-type">Float</span><br><span class="hljs-title">deriving</span> <span class="hljs-type">Repr</span> <br><br><span class="hljs-meta">#check Point2D.point -- Point2D.point (x y : Float) : Point2D</span><br></code></pre></td></tr></table></figure><p>(<code>#check</code> 用来获取一个表达式的类型)</p><p>我们可以用句点来访问分量</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-meta">#eval origin.x -- 0</span><br><span class="hljs-meta">#eval origin.y -- 0</span><br></code></pre></td></tr></table></figure><h3 id="归纳类型">归纳类型</h3><p>我们现在来介绍归纳类型, 在别的语言中, 这也常称为代数数据类型,我们可以看一下 Lean 中的布尔型是如何定义的</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">inductive</span> <span class="hljs-type">Bool</span> <span class="hljs-keyword">where</span><br>  | false : <span class="hljs-type">Bool</span><br>  | true : <span class="hljs-type">Bool</span><br></code></pre></td></tr></table></figure><p>其中 <code>Bool</code> 是定义的类型的名字, <code>true</code> 和<code>false</code> 是两个构造子, 构造子指明了如何构造一个<code>Bool</code>, 在这里有两个构造子, 意味着有两个方法来构造<code>Bool</code> 类型. 如下</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">Bool</span>.true : <span class="hljs-type">Bool</span><br><span class="hljs-type">Bool</span>.false : <span class="hljs-type">Bool</span><br></code></pre></td></tr></table></figure><p>不用我说, Haskell 用户就知道这是什么; Rust 用户可能会记起他们的<code>enum</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 这是 Rust</span><br><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Bool</span> &#123;<br>  True, <br>  False,<br>&#125;<br></code></pre></td></tr></table></figure><p>为了和归纳类型一起工作, Lean 和大多数语言一样都提供了模式匹配的语法,比如我们可以这样来写一个 <code>not</code> 函数</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> myNot (b : <span class="hljs-type">Bool</span>) :=<br>  match b with<br>  | <span class="hljs-type">Bool</span>.true =&gt; <span class="hljs-type">Bool</span>.false<br>  | <span class="hljs-type">Bool</span>.false =&gt; <span class="hljs-type">Bool</span>.true<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> myNot : <span class="hljs-type">Bool</span> → <span class="hljs-type">Bool</span> <br>| <span class="hljs-type">Bool</span>.true =&gt; <span class="hljs-type">Bool</span>.false<br>| <span class="hljs-type">Bool</span>.false =&gt; <span class="hljs-type">Bool</span>.true<br></code></pre></td></tr></table></figure><p>为了适配不同人群的需求, Lean 经常会对同一个功能定义不同的语法,上面两种定义实际没有区别, 我们以第一种为例来说明, 此句使用了<code>match</code> 语句, 对 <code>b</code> 匹配, 提供两条通路, 如果<code>b</code> 可以被 <code>Bool.true</code> 匹配就会走第一条路, 通向<code>Bool.false</code>, 如果第一条不匹配,就去匹配<code>Bool.false</code>, 匹配成功就会通向<code>Bool.true</code>. <code>Bool</code> 的定义只有两种构造子,这表明这两种构造子一定可以覆盖 <code>b</code> 的所有情况.</p><hr /><p>现在我们考虑一个 <code>Shape</code> 类型, 我们可能会有这种需求:一个形状可能是确定长和宽的矩形, 也可能是给定半径的圆形.我们可以构造如下类型来表示这个需求</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">inductive</span> <span class="hljs-type">Shape</span> <span class="hljs-keyword">where</span><br>  | rect (a b : <span class="hljs-type">Float</span>) : <span class="hljs-type">Shape</span><br>  | circ (r : <span class="hljs-type">Float</span>)   : <span class="hljs-type">Shape</span><br><span class="hljs-title">deriving</span> <span class="hljs-type">Repr</span><br></code></pre></td></tr></table></figure><p>然后定义一个计算面积的函数来展示我们如何进行模式匹配</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> area (s : <span class="hljs-type">Shape</span>) := match s with<br>  | <span class="hljs-type">Shape</span>.rect a b =&gt; a * b<br>  | <span class="hljs-type">Shape</span>.circ r =&gt; <span class="hljs-number">3.14</span> * r * r<br><br><span class="hljs-meta">#eval area (Shape.rect 2 3) -- 6.000000</span><br><span class="hljs-meta">#eval area (Shape.circ 1)   -- 3.140000</span><br></code></pre></td></tr></table></figure><h3 id="归纳的类型和递归">归纳的类型和递归</h3><p>我们可以归纳地定义一个类型, 比如标准库里的自然数是这样定义的</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">inductive</span> <span class="hljs-type">Nat</span> <span class="hljs-keyword">where</span><br>  | zero : <span class="hljs-type">Nat</span><br>  | succ (n : <span class="hljs-type">Nat</span>) : <span class="hljs-type">Nat</span><br></code></pre></td></tr></table></figure><p>这个类型令人惊奇的地方在于第二个构造子 <code>succ</code> 的参数居然是<code>Nat</code> 类型本身, 而我们现在正在定义 <code>Nat</code>,这样我们可以这样构造类型</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">open</span> <span class="hljs-type">Nat</span> <span class="hljs-comment">-- 打开Nat命名空间, 这样我们就可以直接写 zero 和 succ 而不是 Nat.zero ...</span><br><br><span class="hljs-meta">#check zero -- 这代表 0</span><br><span class="hljs-meta">#check succ zero -- 这代表 1</span><br><span class="hljs-meta">#check succ (succ zero) -- 这代表 2</span><br>...<br></code></pre></td></tr></table></figure><p>现在我们可以递归地定义如下函数</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> myAdd (m n : <span class="hljs-type">Nat</span>) := match m with<br>  | zero    =&gt; n<br>  | succ pm =&gt; succ (myAdd pm n)<br></code></pre></td></tr></table></figure><p>Lean 和其他的证明助手一样, 都要求你的递归必须是显然能够终止的, 即,你每次递归调用的参数必须在一步步"减小", 比如这里定义的<code>myAdd</code>, 每次递归的时候, 它的第一个参数就在不断地减小, 此时Lean 就能看出来它是明显终止的, 倘若一个函数终止的并不明显,比如下面的函数</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> checkCollatz (n : <span class="hljs-type">Nat</span>) : <span class="hljs-type">Unit</span> :=<br>  <span class="hljs-keyword">if</span> n == <span class="hljs-number">1</span> || n == <span class="hljs-number">0</span> <span class="hljs-keyword">then</span><br>    ()<br>  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> <span class="hljs-type">Nat</span>.mod n <span class="hljs-number">2</span> == <span class="hljs-number">0</span> <span class="hljs-keyword">then</span><br>    checkCollatz (<span class="hljs-type">Nat</span>.div n <span class="hljs-number">2</span>)<br>  <span class="hljs-keyword">else</span><br>    checkCollatz (<span class="hljs-number">3</span> * n + <span class="hljs-number">1</span>)<br></code></pre></td></tr></table></figure><p>如果角谷猜想成立, 那么这个函数必然在有限步内终止, Lean 当然看不出来,不允许你编译此函数. 但我们确实需要这个函数, 这时候我们只需要在<code>def</code> 前面加一个 <code>partial</code> 告诉 Lean这个函数有可能是不完全的(在一些数值上没有良好定义).</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">partial</span> def checkCollatz (n : <span class="hljs-type">Nat</span>) : <span class="hljs-type">Unit</span> :=<br>  <span class="hljs-keyword">if</span> n == <span class="hljs-number">1</span> || n == <span class="hljs-number">0</span> <span class="hljs-keyword">then</span><br>    ()<br>  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> <span class="hljs-type">Nat</span>.mod n <span class="hljs-number">2</span> == <span class="hljs-number">0</span> <span class="hljs-keyword">then</span><br>    checkCollatz (<span class="hljs-type">Nat</span>.div n <span class="hljs-number">2</span>)<br>  <span class="hljs-keyword">else</span><br>    checkCollatz (<span class="hljs-number">3</span> * n + <span class="hljs-number">1</span>)<br></code></pre></td></tr></table></figure><p>不完全的函数是通用编程的客观需要, 比如我们的一个服务器程序,我们需要它主动进入一个死循环, 而很多函数的终止性很难证明,所以与其花时间证明它终止, 不如直接写个 <code>partial</code>.</p><p>Lean 中的列表定义如下</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">inductive</span> <span class="hljs-type">List</span> α <span class="hljs-keyword">where</span><br>  | nil : <span class="hljs-type">List</span> α<br>  | cons (head : α) (tail : <span class="hljs-type">List</span> α) : <span class="hljs-type">List</span> α<br></code></pre></td></tr></table></figure><p>其中 <code>α</code> 是一个类型参数, 在其他语言里, 可能写作<code>List&lt;α&gt;</code>, 这个列表的定义和自然数异曲同工,不再赘述.</p><p>然后这里还有两个常用类型</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">inductive</span> <span class="hljs-type">Sum</span> (α : <span class="hljs-type">Type</span> _) (β : <span class="hljs-type">Type</span> _) <span class="hljs-keyword">where</span><br>  | inl (val : α) : <span class="hljs-type">Sum</span> α β<br>  | inr (val : β) : <span class="hljs-type">Sum</span> α β<br><br><span class="hljs-title">structure</span> <span class="hljs-type">Prd</span> (α : <span class="hljs-type">Type</span> _) (β : <span class="hljs-type">Type</span> _) <span class="hljs-keyword">where</span><br>  fst : α<br>  snd : β<br></code></pre></td></tr></table></figure><p>其中 <code>(α : Type _) (β : Type _)</code> 表明 <code>α β</code>是两个类型, 在前面的例子中我们省略了这个标注(Lean可以推出来),下划线意味着这里省略了一个类型层级的标注, 一般编程无需关注.</p><p>第一个类型是 Rust 人熟悉的 <code>Result</code>, Haskell中叫做<code>Either</code>, 第二个是我们大家都熟悉的二元组, 可以用<code>(x,y)</code> 快速构造一个二元组, 多元组就是二元组的嵌套, 即<code>(x, (y, z))</code>, Lean 允许我们简记作 <code>(x,y,z)</code>.</p><h2 id="lean4-是一个命令式语言">Lean4 是一个"命令式语言"</h2><p>正如很多纯函数式语言做的那样, Lean4 将输入输出功能放在一个称为<code>IO</code> 的上下文里, 得益于 <code>do</code> notation,你不必了解任何单子相关知识也能使用 IO 操作.</p><p>如下是一个简单的计算 a + b 的程序</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> main : <span class="hljs-type">IO</span> <span class="hljs-type">Unit</span> := <span class="hljs-keyword">do</span><br>  <span class="hljs-keyword">let</span> stdin ← <span class="hljs-type">IO</span>.getStdin<br>  <span class="hljs-keyword">if</span> <span class="hljs-keyword">let</span> [some a,some b] := <span class="hljs-type">Functor</span>.map <span class="hljs-type">String</span>.toNat? ((←stdin.getLine).split (· == <span class="hljs-string">&#x27; &#x27;</span>)) <span class="hljs-keyword">then</span><br>    <span class="hljs-type">IO</span>.println (a + b)<br>  <span class="hljs-keyword">else</span><br>    <span class="hljs-type">IO</span>.println <span class="hljs-string">&quot;Read Error&quot;</span><br></code></pre></td></tr></table></figure><p><code>IO α</code> 可以看作一个装着一个 <code>α</code> 的箱子,这个箱子只能在 <code>IO</code>的上下文(比如这里的<code>do</code>之后的上下文)内打开,打开的方法就是使用 <code>←</code>. 比如这里的 <code>IO.getStdin</code> ,它的类型是 <code>IO Stream</code>, 所以我们可以用 <code>←</code>把里面的 <code>Stream</code> 取出来放到变量 <code>stdin</code> 里.</p><p><code>do</code> 可以出现在任何需要用到类型为 <code>IO α</code>的地方(实际上它有更广泛的适用条件), 写下 <code>do</code>之后就可以想写命令式语言一样写程序. 我们甚至可以定义可变量</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-comment">-- 计算 1 + 2 + ... + 100</span><br><span class="hljs-title">def</span> main : <span class="hljs-type">IO</span> <span class="hljs-type">Unit</span> := <span class="hljs-keyword">do</span><br>  <span class="hljs-keyword">let</span> mut sum := <span class="hljs-number">0</span><br>  for i <span class="hljs-keyword">in</span> <span class="hljs-type">List</span>.range <span class="hljs-number">101</span> <span class="hljs-keyword">do</span><br>    sum := sum + i<br>  <span class="hljs-type">IO</span>.println sum<br></code></pre></td></tr></table></figure><p>令 Haskell 用户惊奇的是 Lean4 可以使用 <code>break</code>跳出循环</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-comment">-- 从一数到一百</span><br><span class="hljs-title">def</span> main : <span class="hljs-type">IO</span> <span class="hljs-type">Unit</span> := <span class="hljs-keyword">do</span><br>  <span class="hljs-keyword">let</span> mut i := <span class="hljs-number">1</span><br>  while <span class="hljs-type">True</span> <span class="hljs-keyword">do</span><br>    <span class="hljs-keyword">if</span> i &gt; <span class="hljs-number">100</span> <span class="hljs-keyword">then</span><br>      break<br>    <span class="hljs-type">IO</span>.println i<br>    i := i + <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>同样 Lean4 可以使用 <code>continue</code> 和 early<code>return</code>.</p><h2 id="lean4-是一个依值类型语言">Lean4 是一个依值类型语言</h2><p>一般的语言中允许像是 <code>List α</code> 这样的类型,这个类型依赖于类型参数 <code>α</code>.那么一个自然的想法是一个类型可不可以依赖于一个值? 答案是肯定的,比如如下的类型</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">inductive</span> <span class="hljs-type">Vec</span> : <span class="hljs-type">Nat</span> → <span class="hljs-type">Type</span> _ → <span class="hljs-type">Type</span> _ <span class="hljs-keyword">where</span><br>  | nil : <span class="hljs-type">Vec</span> <span class="hljs-number">0</span> α<br>  | cons : α → <span class="hljs-type">Vec</span> n α → <span class="hljs-type">Vec</span> (<span class="hljs-type">Nat</span>.succ n) α<br></code></pre></td></tr></table></figure><p><code>Vec</code> 就包含一个自然数参数,并且归纳的规则正好可以限制列表的长度为这个自然数. 利用 <code>Vec</code>类型, 我们可以保证不会触发越界问题.</p><p>我们现在提一个实际编程中的常见问题来展示依值类型的实际作用,API的设计者可能对此感兴趣.</p><p>假如你在使用一个比较复杂的API, 比如 OpenGL,在你开始在屏幕上画图之前需要做很多初始化工作, 比如初始化缓冲区,初始化顶点数据等等, 如果在初始化上做错了些步骤, 编译器不会抱怨,但却会在运行时产生令人迷惑的错误.如果我们想让编译器检查这类错误该怎么办呢?下面通过一个例子展示依值类型是如何解决这个问题的, 这个例子修改自"Type-driven Development with Idris (Edwin Brady)".</p><p>考虑有一扇门, 门有三种状态, 开着, 关着(未锁), 锁着.然后你可以对门做一系列操作, 比如开, 关, 开锁, 上锁.有一些操作是不合理的, 比如试图打开一个开着的门.我们可以定义如下的类型来描述一个对门的操作</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">structure</span> <span class="hljs-type">Key</span> <span class="hljs-keyword">where</span><br>  user : <span class="hljs-type">String</span><br><br><span class="hljs-title">inductive</span> <span class="hljs-type">DoorState</span> <span class="hljs-keyword">where</span> <br>  | <span class="hljs-type">SOpen</span> (key : <span class="hljs-type">Key</span>) | <span class="hljs-type">SClosed</span> (key : <span class="hljs-type">Key</span>) | <span class="hljs-type">SLocked</span><br><span class="hljs-title">open</span> <span class="hljs-type">DoorState</span><br><br><span class="hljs-title">inductive</span> <span class="hljs-type">DoorCmd</span> : (before after : <span class="hljs-type">DoorState</span>) -&gt; <span class="hljs-type">Type</span> _ → <span class="hljs-type">Type</span> _ <span class="hljs-keyword">where</span><br>  | <span class="hljs-type">Open</span>   : <span class="hljs-type">DoorCmd</span> (<span class="hljs-type">SClosed</span> key) (<span class="hljs-type">SOpen</span> key) <span class="hljs-type">Unit</span><br>  | <span class="hljs-type">Close</span>  : <span class="hljs-type">DoorCmd</span> (<span class="hljs-type">SOpen</span> key) (<span class="hljs-type">SClosed</span> key) <span class="hljs-type">Unit</span><br>  | <span class="hljs-type">Lock</span>   : <span class="hljs-type">DoorCmd</span> (<span class="hljs-type">SClosed</span> key) <span class="hljs-type">SLocked</span> <span class="hljs-type">Unit</span><br>  | <span class="hljs-type">Unlock</span> : (key : <span class="hljs-type">Key</span>) → <span class="hljs-type">DoorCmd</span> <span class="hljs-type">SLocked</span> (<span class="hljs-type">SClosed</span> key) <span class="hljs-type">Unit</span><br>  | <span class="hljs-type">GenKey</span> : <span class="hljs-type">String</span> → <span class="hljs-type">DoorCmd</span> α α <span class="hljs-type">Key</span><br>  | pure   : α → <span class="hljs-type">DoorCmd</span> β β α<br>  | bind   : <span class="hljs-type">DoorCmd</span> α β <span class="hljs-type">T</span> → (<span class="hljs-type">T</span> → <span class="hljs-type">DoorCmd</span> β γ <span class="hljs-type">S</span>) → <span class="hljs-type">DoorCmd</span> α γ <span class="hljs-type">S</span><br><span class="hljs-title">open</span> <span class="hljs-type">DoorCmd</span><br></code></pre></td></tr></table></figure><p>其中参数 <code>key</code> 表示插在门上的钥匙.</p><p><code>DoorCmd before after α</code> 表示一个关于门的过程,这个过程要求起始状态为 <code>before</code>, 并且完成过程之后的状态为<code>after</code>, 然后这个过程可以有返回值, 其类型在这里记作<code>α</code>. 这个类型的前几个构造器是基本的对门的操作,剩下的两个构造器, 一个称为 <code>pure</code>,它的作用是把一个值包装成一个返回这个值的过程, <code>bind</code>可以用来连接两个过程,其中第一个过程的返回结果可以在第二个过程中出现(这是类型参数<code>T</code> 的作用).</p><p>熟悉 Haskell 的人可以认出来我们实际在写 Free Monad,软件工程人能看出来我们在试图捏一个 EDLS, 但一般语言里的 EDLS没法保证我们这里所谓的语序安全(实际上 Haskell可以用一些非常的技巧做到这点).</p><p>我们可以组合一个过程</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> <span class="hljs-keyword">proc</span> : <span class="hljs-type">DoorCmd</span> <span class="hljs-type">SLocked</span> <span class="hljs-type">SLocked</span> <span class="hljs-type">Unit</span> :=<br>  bind (<span class="hljs-type">GenKey</span> <span class="hljs-string">&quot;admin&quot;</span>) fun key =&gt;<br>  bind (<span class="hljs-type">Unlock</span> key) fun _ =&gt;<br>  bind <span class="hljs-type">Open</span>  fun _ =&gt;<br>  bind <span class="hljs-type">Close</span> fun _ =&gt;<br>  bind <span class="hljs-type">Lock</span>  fun _ =&gt;<br>  pure ()<br></code></pre></td></tr></table></figure><p>这个过程可以理解作: "一个人使用'admin'作为名字申请了一个钥匙,用它开了锁, 然后打开了门, 又关上了门,最后锁上了门",我们看一下如果他忘记了关门而直接在开门后锁门会发生什么,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> <span class="hljs-keyword">proc</span> : <span class="hljs-type">DoorCmd</span> <span class="hljs-type">SLocked</span> <span class="hljs-type">SLocked</span> <span class="hljs-type">Unit</span> :=<br>  bind (<span class="hljs-type">GenKey</span> <span class="hljs-string">&quot;admin&quot;</span>) fun key =&gt;<br>  bind (<span class="hljs-type">Unlock</span> key) fun _ =&gt;<br>  bind <span class="hljs-type">Open</span> fun _ =&gt;<br>  bind <span class="hljs-type">Lock</span> fun _ =&gt;<br>  pure ()<br><br><span class="hljs-comment">-- application type mismatch</span><br><span class="hljs-comment">--   DoorCmd.bind Lock</span><br><span class="hljs-comment">-- argument</span><br><span class="hljs-comment">--   Lock</span><br><span class="hljs-comment">-- has type</span><br><span class="hljs-comment">--   DoorCmd (SClosed ?m.21251) SLocked Unit : Type 1</span><br><span class="hljs-comment">-- but is expected to have type</span><br><span class="hljs-comment">--   DoorCmd (SOpen key) ?m.21247 ?m.21248 : Type 1</span><br></code></pre></td></tr></table></figure><p>Lean 提示了如上注释中的错误, 这个错误提示告诉我们: <code>Lock</code>是一个从关着的门到锁着的门的一个过程,但这里需要的是一个从开着的门到某个状态的过程.</p><p>这只是我们要做的事情的一半, 另一半是要去执行这个过程,我们可以定义一个输出执行器:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">def</span> interpret : <span class="hljs-type">DoorCmd</span> α β <span class="hljs-type">T</span> → <span class="hljs-type">IO</span> <span class="hljs-type">T</span><br>| <span class="hljs-type">Open</span> (key := key) =&gt; <span class="hljs-type">IO</span>.println s!<span class="hljs-string">&quot;Door opened with &#123;key.user&#125;&#x27;s key.&quot;</span><br>| <span class="hljs-type">Close</span> =&gt; <span class="hljs-type">IO</span>.println <span class="hljs-string">&quot;Door closed.&quot;</span><br>| <span class="hljs-type">Lock</span> =&gt; <span class="hljs-type">IO</span>.println <span class="hljs-string">&quot;Door locked.&quot;</span><br>| <span class="hljs-type">Unlock</span> ⟨ user ⟩ =&gt; <span class="hljs-type">IO</span>.println s!<span class="hljs-string">&quot;Door unlocked by &#123;user&#125;.&quot;</span><br>| <span class="hljs-type">GenKey</span> user =&gt; <span class="hljs-keyword">do</span><br>    <span class="hljs-type">IO</span>.println s!<span class="hljs-string">&quot;Generated a key for &#123;user&#125;.&quot;</span><br>    return (<span class="hljs-type">Key</span>.mk user);<br>| <span class="hljs-type">DoorCmd</span>.pure x =&gt; <span class="hljs-keyword">do</span> return x<br>| <span class="hljs-type">DoorCmd</span>.bind m f =&gt; <span class="hljs-keyword">do</span><br>    <span class="hljs-keyword">let</span> x &lt;- interpret m<br>    interpret (f x)<br></code></pre></td></tr></table></figure><p>这个解释器可以把每一步的过程打印出来</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-meta">#eval interpret proc</span><br><span class="hljs-comment">-- print:</span><br><span class="hljs-comment">--  Generated a key for admin.</span><br><span class="hljs-comment">--  Door unlocked by admin.</span><br><span class="hljs-comment">--  Door opened with admin&#x27;s key.</span><br><span class="hljs-comment">--  Door closed.</span><br><span class="hljs-comment">--  Door locked.</span><br></code></pre></td></tr></table></figure><p>如果你是在写一个类型安全的 OpenGL API包装, 那你就要为你的 API设计一个状态类型, 然后考虑如何构造每一个过程, 并把你的<code>interpret</code> 函数和实际的底层操作联系起来.</p><p>实际上 Lean4 有极强的宏功能, 我们可以通过自定义语法让我们的 EDSL用起来更舒服</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">notation</span> <span class="hljs-string">&quot;let &quot;</span> e <span class="hljs-string">&quot; &lt;- &quot;</span> exp <span class="hljs-string">&quot;in&quot;</span> other =&gt; <span class="hljs-type">DoorCmd</span>.bind exp (fun e =&gt; other)<br><span class="hljs-title">notation</span> e1 <span class="hljs-string">&quot;;&quot;</span> e2 =&gt; (<span class="hljs-keyword">let</span> _ &lt;- e1 <span class="hljs-keyword">in</span> e2)<br><span class="hljs-title">notation</span> e <span class="hljs-string">&quot;;&quot;</span> =&gt; e ; pure ()<br><br><span class="hljs-title">def</span> <span class="hljs-keyword">proc</span>&#x27; : <span class="hljs-type">DoorCmd</span> <span class="hljs-type">SLocked</span> <span class="hljs-type">SLocked</span> <span class="hljs-type">Unit</span> :=<br>  <span class="hljs-keyword">let</span> key &lt;- <span class="hljs-type">GenKey</span> <span class="hljs-string">&quot;admin&quot;</span> <span class="hljs-keyword">in</span><br>  <span class="hljs-type">Unlock</span> key;<br>  <span class="hljs-type">Open</span>;<br>  <span class="hljs-type">Close</span>;<br>  <span class="hljs-type">Lock</span>;<br></code></pre></td></tr></table></figure><p>Lean 的宏非常强大, Lean 的 <code>if</code>, <code>while</code>,<code>do</code>, 等等语句, 甚至包含改变运算级的小括号都不是 Lean语言本身的一部分, 而是用宏实现的.</p><p>这种通过类型系统保证顺序安全的API其实更适合调库侠使用.</p><h2 id="lean4-是一个定理证明器">Lean4 是一个定理证明器</h2><p>Lean4 可以用来表达和证明数学命题, 这是 Lean 的初衷, 即,用来严格地验证数学定理. 它拥有一个庞大的数学定理库 Mathlib,并已在数学界发挥了作用. 作为定理证明器的 Lean 不在本文介绍范围内, 请参见<a href="https://lean-lang.org/theorem_proving_in_lean4/"class="uri">https://lean-lang.org/theorem_proving_in_lean4/</a>.</p><h2 id="结">结</h2><p>如果读者对这款新的编程语言感兴趣, 这里有一本详细介绍 Lean4的编程语言特性的在线教程 <ahref="https://lean-lang.org/functional_programming_in_lean/"class="uri">https://lean-lang.org/functional_programming_in_lean/</a>,它包含了本文没有介绍的内容, 比如类型类(Rust 和 Scala 用户会叫它 Trait)和单子及其变换, 还有关于依值类型的更多细节的用途.</p><p>由于刚刚起步, Lean作为编程语言的生态很差,但是正如一位伟人说的那样，面包会有的，牛奶会有的，一切都会有的.</p>]]></content>
    
    
    
    <tags>
      
      <tag>编程</tag>
      
      <tag>计算机</tag>
      
      <tag>Lean</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>统计力学笔记</title>
    <link href="/2023/11/16/%E7%BB%9F%E8%AE%A1%E5%8A%9B%E5%AD%A6%E7%AC%94%E8%AE%B0/"/>
    <url>/2023/11/16/%E7%BB%9F%E8%AE%A1%E5%8A%9B%E5%AD%A6%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<div class="row">    <embed src="https://konjacsource.github.io/pdfs/统计力学笔记.pdf" width="100%" height="1500px" type="application/pdf"></div>]]></content>
    
    
    
    <tags>
      
      <tag>量子力学</tag>
      
      <tag>统计力学</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>海森堡方程</title>
    <link href="/2023/11/12/Heisenberg/"/>
    <url>/2023/11/12/Heisenberg/</url>
    
    <content type="html"><![CDATA[<div class="row">    <embed src="https://konjacsource.github.io/pdfs/海森堡绘景.pdf" width="100%" height="1500px" type="application/pdf"></div>]]></content>
    
    
    
    <tags>
      
      <tag>量子力学</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>类型体操指北 (Haskell)</title>
    <link href="/2023/11/08/Haskell/"/>
    <url>/2023/11/08/Haskell/</url>
    
    <content type="html"><![CDATA[<p>作为类型系统最复杂的非DT语言之一, Haskell有着强大的类型编程能力.我们将从最简单的构造开始展示这些能力, 本文假定读者了解Haskell的基本语法.本文使用的版本是GHC 9.0.2, 但在较旧的版本上这些代码也能运行.</p><p>首先我们需要打开以下拓展</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-meta">&#123;-# LANGUAGE MultiParamTypeClasses #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE FlexibleInstances  #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE TypeApplications #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE DataKinds #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE TypeFamilies #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE TypeOperators #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE KindSignatures #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE GADTs #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE UndecidableInstances #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE AllowAmbiguousTypes #-&#125;</span><br></code></pre></td></tr></table></figure><p>并导入</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-keyword">import</span> Data.Kind(<span class="hljs-type">Type</span>)<br></code></pre></td></tr></table></figure><p>读者不必完全熟悉这些拓展, 我们会慢慢介绍一些特别的语法.</p><h2 id="value-type-kind">Value, Type, Kind</h2><p>我们已经很熟悉Haskell中的一系列值(value), 比如<code>2,3,True,(\x -&gt; x * 2)</code>. 它们有各自所属的类型(type)</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-number">2</span>             :: <span class="hljs-type">Num</span> a =&gt; a <br><span class="hljs-number">3</span>             :: <span class="hljs-type">Num</span> a =&gt; a<br><span class="hljs-type">True</span>          :: <span class="hljs-type">Bool</span><br>(\x -&gt; x * <span class="hljs-number">2</span>) :: <span class="hljs-keyword">forall</span> a. <span class="hljs-type">Num</span> a =&gt; a -&gt; a<br></code></pre></td></tr></table></figure><p>一个自然的想法是把类型也看作另一种值, 它们也有自己所属的类型,此时这个类型就可以称为 Kind. 比如我们会这样记</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">Num</span> a =&gt; a                :: <span class="hljs-type">Type</span><br><span class="hljs-type">Bool</span>                      :: <span class="hljs-type">Type</span> <br><span class="hljs-title">forall</span> a. <span class="hljs-type">Num</span> a =&gt; a -&gt; a :: <span class="hljs-type">Type</span><br></code></pre></td></tr></table></figure><p>其中 <code>Type</code> 是这样一种"类型", 它的元素是所有类型, 比如<code>Int</code>, <code>Int -&gt; Int</code>,<code>forall a. Num a =&gt; a</code>.</p><p><code>Maybe</code> 是 <code>Type</code> 的元素吗? 不是, 但<code>Maybe Int</code> 是, 所以 <code>Maybe</code> 实际是一个<code>Type</code> 到 <code>Type</code> 的函数.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">Int</span>    :: <span class="hljs-type">Type</span><br><span class="hljs-type">Maybe</span>  :: <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span><br>[]     :: <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span><br>(-&gt;)   :: <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span><br><span class="hljs-type">Either</span> :: <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span><br></code></pre></td></tr></table></figure><blockquote><p>在 ghci 中使用 <code>:k t</code> 查看类型<code>t</code>所属的kind,输出结果中 <code>Type</code> 被记作 *.</p></blockquote><p>我不知道 Kind 有什么好翻译, 所以干脆不翻译.</p><h2 id="类型层面的adt">类型层面的ADT</h2><p>读者一定很熟悉一些Haskell中的ADT, 比如</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Bool</span> = <span class="hljs-type">True</span> | <span class="hljs-type">False</span></span><br></code></pre></td></tr></table></figure><p>其中 <code>Bool</code> 是类型, <code>True</code> 和<code>False</code> 是值, 我们希望把它们统一提高一层,这样我们就能在类型编程时用上它们. <code>DataKinds</code>拓展允许我们这样做. 只要开启这个拓展,Haskell就会自动为所有的ADT(包括自定义的和其他模块里的)创建一个高一层的副本.以 <code>Bool</code> 为例, 打开 <code>DataKinds</code> 时,Haskell自动创建一个Kind <code>Bool</code>, 两个类型<code>'True 'False :: Bool</code>,这里单引号是用于区分值<code>True</code>值<code>False</code>和类型<code>'True</code>和类型<code>'False</code>(另一方面你永远不用担心类型<code>Bool</code>和Kind<code>Bool</code>会混淆).这样就有,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs haskell">&#x27;<span class="hljs-type">True</span>                :: <span class="hljs-type">Bool</span><br>&#x27;<span class="hljs-type">False</span>               :: <span class="hljs-type">Bool</span><br>&#x27;[]                  :: [a]<br>&#x27;[ &#x27;<span class="hljs-type">True</span>, &#x27;<span class="hljs-type">False</span> ]   :: [<span class="hljs-type">Bool</span>]<br>&#x27;<span class="hljs-type">True</span> &#x27;: &#x27;[]         :: [<span class="hljs-type">Bool</span>] <span class="hljs-comment">-- 这里需要打开 TypeOperators</span><br>&#x27;<span class="hljs-type">Just</span> <span class="hljs-type">True</span>           :: <span class="hljs-type">Maybe</span> <span class="hljs-type">Bool</span> <br>&#x27;<span class="hljs-type">Just</span> &#x27;<span class="hljs-type">True</span>          :: <span class="hljs-type">Maybe</span> <span class="hljs-type">Bool</span> <span class="hljs-comment">-- 注意这两个 Maybe Bool 实际上不同</span><br>&#x27;<span class="hljs-type">Just</span> <span class="hljs-type">Bool</span>           :: <span class="hljs-type">Maybe</span> <span class="hljs-type">Type</span><br>&#x27;( &#x27;<span class="hljs-type">True</span>, &#x27;<span class="hljs-type">Nothing</span> ) :: (<span class="hljs-type">Bool</span>, <span class="hljs-type">Maybe</span> a)<br></code></pre></td></tr></table></figure><h3 id="自然数">自然数</h3><p>我们熟悉的数字类型 <code>Int, Integer, Float, Double</code>都不是ADT, 无法被 <code>DataKinds</code> 提升,我们自然的需求是搞一个能用的数字类型. 最简单的当然是我们的自然数.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Nat</span> = <span class="hljs-type">Z</span> | <span class="hljs-type">S</span> <span class="hljs-type">Nat</span> <span class="hljs-keyword">deriving</span> <span class="hljs-type">Show</span></span><br></code></pre></td></tr></table></figure><p>其中 <code>Z</code> 代表数字 <span class="math inline">\(0\)</span>,<code>S k</code> 代表自然数 <span class="math inline">\(k+1\)</span>,其中这样我们可以用 <code>S (S (S Z))</code> 代表数字 <spanclass="math inline">\(3\)</span>.读者可以尝试自行实现自然数上的加法和乘法以作练习.</p><h2 id="类型层面的数据结构">类型层面的数据结构</h2><h3 id="异构列表heterogeneous-list">异构列表(Heterogeneous List)</h3><p>类型列表, Haskell 中的列表类型 <code>[]</code> 就是 ADT,所以它可以被自然提高到类型层面. 我们非常关注Kind <code>[Type]</code> ,它实际是一个由类型构成的列表. 有了这个它, 我们可以在 Haskell中实现异构列表(Heterogeneous List).</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-keyword">infixr</span> <span class="hljs-number">4</span> :+:<br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">HList</span> :: [<span class="hljs-type">Type</span>] -&gt; <span class="hljs-type">Type</span> where <span class="hljs-comment">-- 这里需要打开 GADTs 和 KindSignatures</span></span><br>  <span class="hljs-type">HNil</span>  :: <span class="hljs-type">HList</span> &#x27;[]<br>  (:+:) :: t -&gt; <span class="hljs-type">HList</span> ts -&gt; <span class="hljs-type">HList</span> (t &#x27;: ts)<br></code></pre></td></tr></table></figure><p>这里给不熟悉 GADT 拓展的朋友们解释一下, 第一行是声明运算符的运算级,没什么好说的; 第二行声明了 <code>HList</code> 的 Kind, 接受一个类型列表,返回一个类型; <code>where</code> 之后的两行给出两个构造器,<code>HNil</code> 相当于一个空列表, <code>:+:</code>相当于将一个元素添加到列表头. 重点在于构造器的类型, GADT拓展的用途就是允许我们更精细地控制类型, <code>:+:</code> 的类型告诉我们,它接受两个参数, 一个是任意类型 <code>t</code> 的元素, 一个是<code>HList ts</code> 的元素(其中 <code>ts</code> 是任意类型列表),然后返回一个 <code>HList (t ': ts)</code>.</p><p>于是,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-number">1</span> :+: <span class="hljs-type">True</span> :+: <span class="hljs-type">Just</span> <span class="hljs-number">2</span> :+: <span class="hljs-type">Nothing</span> :+: <span class="hljs-type">HNil</span><br>  :: (<span class="hljs-type">Num</span> n1, <span class="hljs-type">Num</span> n2) =&gt; <span class="hljs-type">HList</span> &#x27;[n1, <span class="hljs-type">Bool</span>, <span class="hljs-type">Maybe</span> n2, <span class="hljs-type">Maybe</span> a]<br></code></pre></td></tr></table></figure><p>为了让我们的 <code>HList</code> 能够正常使用,我们先来定义一些常用函数. 首先是 <code>show</code>,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Show</span> (<span class="hljs-type">HList</span> &#x27;[]) <span class="hljs-keyword">where</span></span><br>  show <span class="hljs-type">HNil</span> = <span class="hljs-string">&quot;HNil&quot;</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> (<span class="hljs-type">Show</span> <span class="hljs-title">t</span>, <span class="hljs-type">Show</span> (<span class="hljs-type">HList</span> <span class="hljs-title">ts</span>)) =&gt; <span class="hljs-type">Show</span> (<span class="hljs-type">HList</span> (<span class="hljs-title">t</span> &#x27;: <span class="hljs-title">ts</span>)) <span class="hljs-keyword">where</span></span><br>  show (x :+: xs) = show x ++ <span class="hljs-string">&quot; :+: &quot;</span> ++ show xs<br></code></pre></td></tr></table></figure><p>这段代码实质上是利用 Haskell 的类型类机制在类型上进行模式匹配,每一个模式都要提供一个instance, 注意这里 Haskell 不会提供模式完整性检查,请自己确保自己定义的instance能覆盖所有可能的情况.</p><p>现在我们试着来做一下<code>HList</code>的拼接, 首先考虑类型要怎么变化,显然链表拼接的类型为</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">hAppend</span> :: <span class="hljs-type">HList</span> ls1 -&gt; <span class="hljs-type">HList</span> ls2 -&gt; <span class="hljs-type">HList</span> (ls1 ++ ls2)<br></code></pre></td></tr></table></figure><p>其中 <code>++</code> 是普通列表的拼接, 但是 <code>DataKinds</code>不会帮我们把一般的函数提升到类型层面,我们需要手动实现类型列表的拼接.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> <span class="hljs-type">TAppend</span> (<span class="hljs-title">xs</span> :: [<span class="hljs-type">Type</span>]) (<span class="hljs-title">ys</span> :: [<span class="hljs-type">Type</span>]) :: [<span class="hljs-type">Type</span>] where</span><br>  <span class="hljs-type">TAppend</span> &#x27;[] ys = ys<br>  <span class="hljs-type">TAppend</span> (x &#x27;: xs) ys = x &#x27;: <span class="hljs-type">TAppend</span> xs ys<br></code></pre></td></tr></table></figure><p>这里毫无疑问要使用 <code>TypeFamilies</code> 拓展,这个拓展允许我们来写类型层面的函数. 一些语言(比如C++, Scala3)有typefamily类似物, 这些语言的类型系统往往都是图灵完备的.</p><p>有了 <code>TAppend</code> ,我们再使用先前的技巧在类型上进行模式匹配</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">HAppend</span> (<span class="hljs-title">ts1</span> :: [<span class="hljs-type">Type</span>]) <span class="hljs-keyword">where</span></span><br>  hAppend :: <span class="hljs-type">HList</span> ts1 -&gt; <span class="hljs-type">HList</span> ts2 -&gt; <span class="hljs-type">HList</span> (<span class="hljs-type">TAppend</span> ts1 ts2)<br><span class="hljs-class">  </span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">HAppend</span> &#x27;[] <span class="hljs-keyword">where</span></span><br>  hAppend <span class="hljs-type">HNil</span> ys = ys<br><span class="hljs-class">  </span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> (<span class="hljs-type">HAppend</span> <span class="hljs-title">ts</span>) =&gt; <span class="hljs-type">HAppend</span> (<span class="hljs-title">t</span> &#x27;: <span class="hljs-title">ts</span>) <span class="hljs-keyword">where</span></span><br>  hAppend (x :+: xs) ys = x :+: hAppend xs ys<br></code></pre></td></tr></table></figure><p>这里 <code>class</code> 起到了标注类型的作用, <code>instance</code>相当于按模式依次进行实现.</p><p>现在使用一个例子来展示异构列表的作用.</p><p>众所周知, Haskell中的 <code>sequence</code> 非常常用, 它的类型是<code>sequence :: Monad m =&gt; [m a] -&gt; m [a]</code>(这不是最一般的类型,但我们现在只考虑这个特化版本).形象一点说就是把列表里的<code>m</code>"翻"出来. 这里用的是一般列表,我们实际可以写一个异构列表版本的 <code>sequence</code>, 这里称为<code>hSequence</code>. 先考虑<code>hSequence</code>的类型是什么,一个自然的想法是</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">hSequence</span> :: <span class="hljs-type">Moand</span> m =&gt; <span class="hljs-type">HList</span> (map m ts) -&gt; m (<span class="hljs-type">HList</span> ts)<br></code></pre></td></tr></table></figure><p>同样我们需要把 <code>map</code> 提到类型层面</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> <span class="hljs-type">Map</span> (<span class="hljs-title">f</span> :: <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span>) (<span class="hljs-title">ts</span> :: [<span class="hljs-type">Type</span>]) :: [<span class="hljs-type">Type</span>] where</span><br>  <span class="hljs-type">Map</span> f &#x27;[] = &#x27;[]<br>  <span class="hljs-type">Map</span> f (t &#x27;: ts) = f t &#x27;: <span class="hljs-type">Map</span> f ts<br></code></pre></td></tr></table></figure><p>然后定义函数</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">hAppend</span> :: <span class="hljs-type">HList</span> ts1 -&gt; <span class="hljs-type">HList</span> ts2 -&gt; <span class="hljs-type">HList</span> (<span class="hljs-type">TAppend</span> ts1 ts2)<br><span class="hljs-title">hAppend</span> <span class="hljs-type">HNil</span> ys = ys<br><span class="hljs-title">hAppend</span> (x :+: xs) ys = x :+: hAppend xs ys<br></code></pre></td></tr></table></figure><p>大功告成.</p><h3 id="定长列表vector">定长列表(Vector)</h3><p>我们经常面对下标越界异常, 是否有办法可以在编译期完全杜绝下标越界呢?答案是肯定的, 这首先需要我们构造一个类型,这个类型能携带列表的长度信息.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-keyword">infixr</span> <span class="hljs-number">4</span> :::<br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Vect</span> :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">VNil</span> :: <span class="hljs-type">Vect</span> &#x27;<span class="hljs-type">Z</span> a<br>  (:::) :: a -&gt; <span class="hljs-type">Vect</span> n a -&gt; <span class="hljs-type">Vect</span> (&#x27;<span class="hljs-type">S</span> n) a<br></code></pre></td></tr></table></figure><p>这里我们用到了之前DataTypes帮我们提升的自然数Kind.</p><p>用自然语言翻译一下:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell">类型 <span class="hljs-type">Vect</span> n a 代表长度为 n 元素类型为 a 的列表构成的类型<br>  <span class="hljs-type">VNil</span> 是空列表, 所以它的类型是 <span class="hljs-type">Vect</span> &#x27;<span class="hljs-type">Z</span> a (记得我们之前把 &#x27;<span class="hljs-type">Z</span> 定作 <span class="hljs-number">0</span>)<br>  (x ::: xs) 相当于把元素 x 拼到列表 xs 前面, 并且这里列表的长度增长了一<br></code></pre></td></tr></table></figure><p>同样按照上面的例子, 溜一下列表拼接</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> (:+) (<span class="hljs-title">m</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) :: <span class="hljs-type">Nat</span> where</span><br>  &#x27;<span class="hljs-type">Z</span>     :+ n = n<br>  (&#x27;<span class="hljs-type">S</span> m) :+ n = &#x27;<span class="hljs-type">S</span> (m :+ n)<br><br><span class="hljs-title">vAppend</span> :: <span class="hljs-type">Vect</span> m a -&gt; <span class="hljs-type">Vect</span> n a -&gt; <span class="hljs-type">Vect</span> (m :+ n) a<br><span class="hljs-title">vAppend</span> <span class="hljs-type">VNil</span> ys = ys<br><span class="hljs-title">vAppend</span> (x:::xs) ys = x ::: vAppend xs ys<br></code></pre></td></tr></table></figure><p>这里顺便定义了类型层面的自然数加法, 这个定义我们之后还要用.</p><p>在详细叙述使用Vector的正确姿势前, 我们先介绍一下依值类型.</p><h2 id="依值类型dependent-type">依值类型(Dependent Type)</h2><p>免责声明: 本文仅简单介绍一点依值类型(DT),并解释如何在Haskell中"模拟"依值类型的写法,实质上Haskell并不支持真正的依值类型.并且如果你对依值类型在编程和数学中的应用感兴趣,Haskell以及本文也不是一个好的入门工具,你应该去学习类型论并掌握一门真正的依值类型语言. 如果你对Haskell比较熟悉,Idris和Agda是不错的入门选择, 除此之外, Lean4也十分值得学习.</p><blockquote><p>我们关注一个被称为依值函数的类型(也称作Pi类型),直接的理解就是一个返回值类型依赖于参数值的函数. 这个类型可以记作<code>(x :: T) -&gt; A</code> 其中 <code>T</code> 是一个普通类型,<code>A</code> 是一个(可以)含有 <code>x</code> 的类型Haskell目前不支持这样写法,我们现在需要思考如何在Haskell中模仿这种函数.</p></blockquote><p>在Haskell中模拟DT的技巧被称为 Singletons (好像有人叫单体类型),实质上是构造一个代理类型将类型 (通常是 <code>'Z, 'True</code> 这种)和值联系起来.</p><p>我们来构造一个 <code>Bool</code> 的Singleton.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">SBool</span> :: <span class="hljs-type">Bool</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">STrue</span>  :: <span class="hljs-type">SBool</span> &#x27;<span class="hljs-type">True</span><br>  <span class="hljs-type">SFalse</span> :: <span class="hljs-type">SBool</span> &#x27;<span class="hljs-type">False</span><br></code></pre></td></tr></table></figure><p>称此为"单体"的原因显而易见,类型<code>SBool 'True</code>有且只有一个元素<code>STrue</code>,<code>SBool 'False</code>也只有一个元素<code>SFalse</code>. 这就将<code>b :: Bool</code> 和 <code>SBool b</code> 联系了起来.</p><p>定义Singleton上的函数时要同时定义类型层面和值层面, 比如</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> <span class="hljs-type">Not</span> (<span class="hljs-title">b</span> :: <span class="hljs-type">Bool</span>) :: <span class="hljs-type">Bool</span> where</span><br>  <span class="hljs-type">Not</span> &#x27;<span class="hljs-type">True</span> = &#x27;<span class="hljs-type">False</span><br>  <span class="hljs-type">Not</span> &#x27;<span class="hljs-type">False</span> = &#x27;<span class="hljs-type">True</span><br><br><span class="hljs-title">sNot</span> :: <span class="hljs-type">SBool</span> b -&gt; <span class="hljs-type">SBool</span> (<span class="hljs-type">Not</span> b)<br><span class="hljs-title">sNot</span> <span class="hljs-type">STrue</span>  = <span class="hljs-type">SFalse</span><br><span class="hljs-title">sNot</span> <span class="hljs-type">SFalse</span> = <span class="hljs-type">STrue</span><br></code></pre></td></tr></table></figure><p><code>sNot</code> 就是一个Singleton风格的依值函数, 它相当于<code>(b :: Bool) -&gt; SBool (not b)</code>.</p><p>现在考虑自然数的Singleton类型, 类似<code>SBool</code>,我们可以流畅的写出来.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">SNat</span> :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">SZ</span> :: <span class="hljs-type">SNat</span> &#x27;<span class="hljs-type">Z</span><br>  <span class="hljs-type">SS</span> :: <span class="hljs-type">SNat</span> n -&gt; <span class="hljs-type">SNat</span> (&#x27;<span class="hljs-type">S</span> n)<br></code></pre></td></tr></table></figure><p>显然,</p><ol type="1"><li><code>SNat 'Z</code> 只有一个元素 <code>SZ</code>,</li><li>对于任何一个自然数<code>k :: Nat</code>, 并且 <code>SNat k</code>只有一个元素 <code>sk</code>, 那么 <code>SNat ('S k)</code>也只有一个元素 <code>'SS sk</code>.</li></ol><p>这样数学归纳告诉我们对于任何自然数 <code>n</code> ,<code>SNat n</code> 只有唯一一个元素. 可见 <code>SNat</code>定义得很正确.</p><p>我们来定义一下加法和乘法,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> (:+:) (<span class="hljs-title">m</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) :: <span class="hljs-type">Nat</span> where</span><br>  &#x27;<span class="hljs-type">Z</span>     :+: n = n<br>  (&#x27;<span class="hljs-type">S</span> m) :+: n = &#x27;<span class="hljs-type">S</span> (m :+: n)<br><br><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> (:*:) (<span class="hljs-title">m</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) :: <span class="hljs-type">Nat</span> where</span><br>  &#x27;<span class="hljs-type">Z</span>     :*: _ = &#x27;<span class="hljs-type">Z</span><br>  (&#x27;<span class="hljs-type">S</span> m) :*: n = n :+: (m :*: n)<br><br><span class="hljs-keyword">infixl</span> <span class="hljs-number">6</span> |+|<br>(|+|) :: <span class="hljs-type">SNat</span> m -&gt; <span class="hljs-type">SNat</span> n -&gt; <span class="hljs-type">SNat</span> (m :+: n)<br><span class="hljs-type">SZ</span>   |+| n = n<br><span class="hljs-type">SS</span> m |+| n = <span class="hljs-type">SS</span> (m |+| n)<br><br><span class="hljs-keyword">infixl</span> <span class="hljs-number">7</span> |*|<br>(|*|) :: <span class="hljs-type">SNat</span> m -&gt; <span class="hljs-type">SNat</span> n -&gt; <span class="hljs-type">SNat</span> (m :*: n)<br><span class="hljs-type">SZ</span>   |*| _ = <span class="hljs-type">SZ</span><br><span class="hljs-type">SS</span> m |*| n = n |+| (m |*| n)<br></code></pre></td></tr></table></figure><p>我们用 <code>:+:</code>, <code>:*:</code> 表示类型层面得加和乘, 用<code>|+|</code>, <code>|*|</code> 表示值层面的加和乘.</p><p>为了方便从类型层面的 <code>Nat</code> 向值层面的 <code>SNat</code>进行转换, 我们可以这样做,注意这里又用到了之前对类型进行模式匹配的技巧</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Proxy</span> t = <span class="hljs-type">Proxy</span></span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">ToSNat</span> (<span class="hljs-title">nat</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  toSNat :: <span class="hljs-type">Proxy</span> nat -&gt; <span class="hljs-type">SNat</span> nat<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">ToSNat</span> &#x27;<span class="hljs-type">Z</span> <span class="hljs-keyword">where</span></span><br>  toSNat <span class="hljs-type">Proxy</span> = <span class="hljs-type">SZ</span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">ToSNat</span> n =&gt; <span class="hljs-type">ToSNat</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">n</span>) <span class="hljs-keyword">where</span></span><br>  toSNat <span class="hljs-type">Proxy</span> = <span class="hljs-type">SS</span> (toSNat (<span class="hljs-type">Proxy</span> :: <span class="hljs-type">Proxy</span> n))<br></code></pre></td></tr></table></figure><p><code>Proxy</code> 实际上就是一个携带类型信息的 <code>()</code>,所以说是类型的代理. 然后可以这样使用:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">toSNat</span> (<span class="hljs-type">Proxy</span> :: <span class="hljs-type">Proxy</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>))) <span class="hljs-comment">-- =&gt; SS (SS SZ)</span><br></code></pre></td></tr></table></figure><p>这里类型标注起到了输入类型参数的作用.</p><h3 id="以vector为例">以Vector为例</h3><p>我们继续定义一些 Vector 上的函数来展示我们 Singleton 的强大力量.首先是从列表转换到Vector</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">fromList</span> :: <span class="hljs-type">SNat</span> n -&gt; [a] -&gt; <span class="hljs-type">Vect</span> n a<br><span class="hljs-title">fromList</span> <span class="hljs-type">SZ</span>     _        = <span class="hljs-type">VNil</span><br><span class="hljs-title">fromList</span> (<span class="hljs-type">SS</span> n) (x : xs) = x ::: fromList n xs<br><span class="hljs-title">fromList</span> _ _ = error <span class="hljs-string">&quot;Index out of range&quot;</span><br></code></pre></td></tr></table></figure><p>这个函数需要预先给定长度, 这揭示Haskell里模拟DT的根本性不足,就是不能依赖于运行时的值.</p><p>然后是 <code>replicateV</code>, 它生成一个由重复元素组成的Vector.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">replicateV</span> :: <span class="hljs-type">SNat</span> n -&gt; a -&gt; <span class="hljs-type">Vect</span> n a<br><span class="hljs-title">replicateV</span> <span class="hljs-type">SZ</span>     _ = <span class="hljs-type">VNil</span><br><span class="hljs-title">replicateV</span> (<span class="hljs-type">SS</span> n) x = x ::: replicateV n x<br></code></pre></td></tr></table></figure><h4 id="安全的下标">安全的下标</h4><p>即便安全如 Haskell, 它的列表类型仍是下标不安全的, 你可以写<code>[1,2] !! 2</code> 来触发一个无法被类型系统发现的越界异常.我们尝试用 <code>Vect</code> 改进这一点.</p><p>我们先定义一个可以限定上界的自然数类型</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Fin</span> :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">FZ</span> :: <span class="hljs-type">Fin</span> (&#x27;<span class="hljs-type">S</span> k)<br>  <span class="hljs-type">FS</span> :: <span class="hljs-type">Fin</span> k -&gt; <span class="hljs-type">Fin</span> (&#x27;<span class="hljs-type">S</span> k)<br></code></pre></td></tr></table></figure><p><code>Fin n</code> 是一个以 <code>n</code> 为上界的类型, 它有<code>n</code> 个元素. 以实例来看:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-type">FZ</span> :: <span class="hljs-type">Fin</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>))<br><span class="hljs-type">FS</span> (<span class="hljs-type">FZ</span> :: <span class="hljs-type">Fin</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)) :: <span class="hljs-type">Fin</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>))<br></code></pre></td></tr></table></figure><p>果然只有两个元素, 读者可以自行尝试写一个<code>FS (FS (FZ :: ???)))</code> 并尝试写出<code>???</code>的内容来验证这一点(剧透: 你写不出来).</p><p>这个类型恰好可以被用来当作 <code>Vect</code> 的下标类型.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-keyword">infixl</span> <span class="hljs-number">9</span> !<br>(!) :: <span class="hljs-type">Vect</span> n a -&gt; <span class="hljs-type">Fin</span> n -&gt; a<br>(x ::: _)  ! <span class="hljs-type">FZ</span>     = x<br>(_ ::: xs) ! (<span class="hljs-type">FS</span> f) = xs ! f<br><span class="hljs-comment">-- 写这个函数的时候 GHC 可能会犯二告诉你 pattern non-exhaustive,</span><br><span class="hljs-comment">-- 但是你把全部模式补上之后又会告诉你 pattern inaccessible. </span><br><span class="hljs-comment">-- 我在较新版的 GHC 里遇到了这个问题, 老版里没遇到.</span><br></code></pre></td></tr></table></figure><p>现在我们的的确确可以在编译期保证不会有人写出这样的代码<code>(1:::2:::VNil) ! (FS (FS FZ))</code>.</p><h3 id="定长矩阵">定长矩阵</h3><p>一个矩阵当然可以像向量那样定下长度</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-type">Mat</span> m n a = <span class="hljs-type">Vect</span> m (<span class="hljs-type">Vect</span> <span class="hljs-title">n</span> <span class="hljs-title">a</span>)</span><br></code></pre></td></tr></table></figure><p>矩阵上也能做很多操作, 笔者在学线代时写了一系列矩阵相关函数,详情请见(<a href="https://github.com/KonjacSource/DependentMat"class="uri">https://github.com/KonjacSource/DependentMat</a>),此处不多赘述.</p><h2 id="定理证明">定理证明</h2><p>免责声明: 我们只能在Haskell里装模做样地做一些定理证明,本文也仅供娱乐, 如果你对定理证明感兴趣, 还请学习Agda/Lean4/Coq,对Haskell熟悉的读者可以快速上手Agda, 这里推荐PLFA(<ahref="https://plfa.github.io" class="uri">https://plfa.github.io</a>),并且此书有非常棒的中文翻译(<ahref="https://zhuanlan.zhihu.com/p/77442536"class="uri">https://zhuanlan.zhihu.com/p/77442536</a>, <ahref="https://agda-zh.github.io/PLFA-zh/"class="uri">https://agda-zh.github.io/PLFA-zh/</a>), ;Lean4也是一个体验非常优秀的定理证明器, 很有拿来做通用编程的潜力,有着非常丰富的数学定理库Mathlib; 然后笔者不会Coq, 不知道怎么安利.</p><p>实际上用Haskell的来写定理证明会有大量的语法噪音,本文甚至可能成为劝退文, 所以如果你对这方面内容真的感兴趣,不妨学会一门定理证明器之后再来阅读.</p><p>用编程语言(定理证明器实际也是编程语言)做定理证明的基础是Curry-Howard对应(下文称CH对应),该对应指出: 命题对应类型, 证明对应类型的项. 我们以一个例子来阐述.</p><p>定义如下类型:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">data</span> (:&lt;:) :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">LZ</span> :: &#x27;<span class="hljs-type">Z</span> :&lt;: &#x27;<span class="hljs-type">S</span> n<br>  <span class="hljs-type">LS</span> :: m :&lt;: n -&gt; &#x27;<span class="hljs-type">S</span> m :&lt;: &#x27;<span class="hljs-type">S</span> n<br></code></pre></td></tr></table></figure><p>这个类型对应了小于关系, 在数学上,小于关系可以看作依赖于两个自然数(我们可以不考虑其它数上的小于关系)的命题,借助CH对应(类型⇔命题), 我们将它写作依赖于两个自然数的类型.然后给出构造器(项), 借助CH对应(证明⇔项),我们将其理解为证明小于关系的方式, 将其写作数学语言就可以说</p><ul><li><code>LZ</code> 可以证明 <span class="math inline">\(\forall n \in\mathbb{N}, 0 &lt; n + 1\)</span></li><li>给 <code>LS</code> 一个 <span class="math inline">\(m &lt;n\)</span> 的证明, <code>LS</code> 还你一个<code>m + 1 &lt; n + 1</code> 的证明.</li></ul><p>这就是我们对小于关系的定义,<strong>通过指明如何证明一个关系成立来定义这个关系</strong>.</p><p>假如我们想要证明 <span class="math inline">\(2 &lt; 4\)</span>该如何证明呢?</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">twoLtfour</span> :: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>) :&lt;: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)))<br><span class="hljs-title">twoLtfour</span> = <span class="hljs-type">LS</span> (<span class="hljs-type">LS</span> (<span class="hljs-type">LZ</span> :: &#x27;<span class="hljs-type">Z</span> :&lt;: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)))<br></code></pre></td></tr></table></figure><p>这里为了读者看得清晰标出了 <code>LZ</code> 的类型,实际上Haskell可以自己推出来. 我们看一下这是怎么证明出来的,请把下面代码块中的<code>::</code>读作"证明了".</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-comment">-- 首先</span><br><span class="hljs-type">LZ</span> :: &#x27;<span class="hljs-type">Z</span> :&lt;: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)<br><span class="hljs-comment">-- 然后, 回忆 LS 的类型签名, 它相当于在不等式两边同时加了一</span><br><span class="hljs-type">LS</span> <span class="hljs-type">LZ</span> :: &#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span> :&lt;: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)<br><span class="hljs-comment">-- 最后</span><br><span class="hljs-type">LS</span> (<span class="hljs-type">LS</span> <span class="hljs-type">LZ</span>) :: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>) :&lt;: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)))<br><span class="hljs-comment">-- Q.E.D.</span><br></code></pre></td></tr></table></figure><h3 id="相等性">相等性</h3><p>我们以证明加法交换律作为本文的结束, 在此之前, 先定义相等性.</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-keyword">infixl</span> <span class="hljs-number">4</span> :~:<br><span class="hljs-class"><span class="hljs-keyword">data</span> (:~:) :: k -&gt; k -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">Refl</span> :: a :~: a<br></code></pre></td></tr></table></figure><p>相等性可以作用在任何两个项上(当然我们这里只用到自然数, 所以你把<code>k</code> 换成 <code>Nat</code> 也不打紧), 它唯一的构造器<code>Refl</code> (自反性) 精准地捕捉了两个事物相等的条件, 即,它俩就是一个东西.</p><p>我们定义三个常用操作</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">sym</span> :: a :~: b -&gt; b :~: a<br><span class="hljs-title">sym</span> <span class="hljs-type">Refl</span> = <span class="hljs-type">Refl</span><br><br><span class="hljs-title">trans</span> :: a :~: b -&gt; b :~: c -&gt; a :~: c<br><span class="hljs-title">trans</span> <span class="hljs-type">Refl</span> <span class="hljs-type">Refl</span> = <span class="hljs-type">Refl</span><br><br><span class="hljs-title">cong</span> :: a :~: b -&gt; f a :~: f b<br><span class="hljs-title">cong</span> <span class="hljs-type">Refl</span> = <span class="hljs-type">Refl</span><br></code></pre></td></tr></table></figure><p>它们的名字依次是对称性, 传递性, 合同性.</p><p>对称性 <code>sym</code> 的类型是 <code>a :~: b -&gt; b :~: a</code>即, 你给它一个 <span class="math inline">\(a = b\)</span> 的证明,它给你一个 <span class="math inline">\(b = a\)</span> 的证明,这就是所谓的蕴含(现在你知道函数对应蕴含了).我们来看一下对称性是如何被证明的,</p><p>当你写下如下代码时</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">sym</span> x = _<br></code></pre></td></tr></table></figure><p>Haskell 会期望下划线处的类型是 <code>b :~: a</code>; 但当你把<code>x</code> 换成 <code>Refl</code> 时, 期望的类型就变成了<code>a :~: a</code>, 这时候就可以直接把 <code>Refl</code> 填进去了.这中间发生了什么? 其实是当你用 <code>Refl</code> 做模式匹配的时候,Haskell就会化简类型, 它会看到 <code>Refl</code> 的类型是<code>a :~: a</code>, 这时候它就知道该把变元 <code>b</code> 换成<code>a</code>, 于是最终的结果类型就变成了 <code>a :~: a</code>.</p><p><code>trans</code> 和 <code>cong</code> 的原理也是同样的,读者可自行验证.</p><h4 id="结合律">结合律</h4><p>加法结合律是 <span class="math inline">\(\forall (a, b, c \in\mathbb{N}), (a + b) + c = a + (b + c)\)</span>, 写成类型就是</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">assoc</span> :: ((a :+: b):+: c) :~: (a :+: (b :+: c)) <span class="hljs-comment">-- Haskell帮我们写了前面的&quot;forall&quot;</span><br></code></pre></td></tr></table></figure><p>由于我们需要在类型上进行模式匹配, 所以开一个类型类,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">Assoc</span> (<span class="hljs-title">a</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">b</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">c</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  assoc :: ((a :+: b) :+: c) :~: (a :+: (b :+: c))<br></code></pre></td></tr></table></figure><p>然后在 <code>a</code> 上做归纳(递归)来证明,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Assoc</span> &#x27;<span class="hljs-type">Z</span> b c <span class="hljs-keyword">where</span></span><br>  assoc = <span class="hljs-type">Refl</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Assoc</span> a b c =&gt; <span class="hljs-type">Assoc</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">a</span>) b c <span class="hljs-keyword">where</span></span><br>  assoc = cong @_ @_ @&#x27;<span class="hljs-type">S</span> (assoc @a @b @c) <span class="hljs-comment">-- 这里要开 TypeApplications</span><br></code></pre></td></tr></table></figure><p>TypeApplications拓展允许我们使用<code>@</code>来把类型参数显式地应用过去.</p><p>首先看第一个情况, <code>Assoc 'Z b c</code>, 这个情况下<code>a = 'Z</code>, 然后Haskell可以计算出<code>((a :+: b) :+: c) = b :+: c</code> 和<code>(a :+: (b :+: c)) = b :+: c</code>, 所以 <code>assoc</code>的类型就化简成了 <code>(b :+: c) :~: (b :+: c)</code>, 用<code>Refl</code> 就直接证明了.</p><p>第二个情况做了归纳, instance的声明表明 "要有<code>Assoc ('S a) b c</code> 的类型类, 就要现有<code>Assoc a b c</code> 的类型类"翻译一下就是"要证明对<code>('S a) b c</code> 的结合律, 需要先证明 <code>a b c</code>的结合律", 这意味着我们可以安全地(不会死循环地)调用<code>assoc @a @b @c</code> ,这实质就是在<code>a</code>上做数学归纳的过程.</p><p>先来看目标类型,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Assoc</span> a b c =&gt; <span class="hljs-type">Assoc</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">a</span>) b c <span class="hljs-keyword">where</span></span><br>  assoc = _<br></code></pre></td></tr></table></figure><p>下划线处的类型应该是<code>((('S a) :+: b) :+: c) :~: (('S a) :+: (b :+: c))</code>, 由<code>:+:</code> 的定义, Haskell可以将其化为最简形式<code>'S ((a :+: b) :+: c) :~: 'S (a :+: (b :+: c))</code>.</p><p>来看一下是如何归纳的,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs haskell">(assoc @a @b @c)                :: ((a :+: b) :+: c) :~: (a :+: (b :+: c))<br><span class="hljs-title">cong</span> @_ @_ @&#x27;<span class="hljs-type">S</span> (assoc @a @b @c) :: &#x27;<span class="hljs-type">S</span> ((a :+: b) :+: c) :~: &#x27;<span class="hljs-type">S</span> (a :+: (b :+: c))<br><span class="hljs-comment">-- 回忆 cong 的类型, 有 (cong @_ @_ @f (xxx :: x :~: y) :: f x :~: f y)</span><br><span class="hljs-comment">-- 相当于在等式两边同时作用一个函数, 以后我们会不写这些参数, 而直接写 cong (assoc @a @b @c)</span><br><span class="hljs-comment">-- Haskell可以推导出来这些参数, 在下面的例子里 f 都是 &#x27;S</span><br></code></pre></td></tr></table></figure><p>这恰好和上面的最简形式一致, 所以我们直接填进去得到</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Assoc</span> a b c =&gt; <span class="hljs-type">Assoc</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">a</span>) b c <span class="hljs-keyword">where</span></span><br>  assoc = cong @_ @_ @&#x27;<span class="hljs-type">S</span> (assoc @a @b @c)<br></code></pre></td></tr></table></figure><p>就像先前提到的, Haskell无法自动检查 <code>Assoc</code>的定义是否覆盖了所有的 <code>Nat</code>, 我们只能人工检查,我们的定义显然是完整覆盖了的,这是Haskell不能作为定理证明器的一个重要原因.定理证明器可以像检查模式匹配完整性那样检查结合律的完整性.另一个重要原因是Haskell不会进行停机检查,你可以轻易地写出无限递归来通过类型检查, 这实际对应了循环论证,定理证明器会确保你的函数一定在有限步内结束.</p><h4 id="交换律">交换律</h4><p>证明交换律之前先证明这样一个小引理: <spanclass="math inline">\(\forall n \in \mathbb{N}, n + 0 = n\)</span>,翻译成类型,</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">Idr</span> (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  idr :: (n :+: &#x27;<span class="hljs-type">Z</span>) :~: n<br></code></pre></td></tr></table></figure><p>证明也非常简单, 鼓励读者自行尝试, 答案如下:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Idr</span> &#x27;<span class="hljs-type">Z</span> <span class="hljs-keyword">where</span></span><br>  idr = <span class="hljs-type">Refl</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Idr</span> n =&gt; <span class="hljs-type">Idr</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">n</span>) <span class="hljs-keyword">where</span></span><br>  idr = cong idr <span class="hljs-comment">-- 完整形式是 cong @(n:+:&#x27;Z) @n @&#x27;S (idr @n), Haskell替我们填写了这些参数</span><br></code></pre></td></tr></table></figure><p>然后是另外一个引理: <span class="math inline">\(\forall (a, b \in\mathbb{N}), a + (b + 1) = (a + b) + 1\)</span>, 翻译成类型</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">SuccR</span> (<span class="hljs-title">a</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">b</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  succR :: (a :+: &#x27;<span class="hljs-type">S</span> b) :~: &#x27;<span class="hljs-type">S</span> (a :+: b)<br></code></pre></td></tr></table></figure><p>对 <code>a</code> 归纳, 有</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">SuccR</span> &#x27;<span class="hljs-type">Z</span> b <span class="hljs-keyword">where</span></span><br>  succR = <span class="hljs-type">Refl</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">SuccR</span> a b =&gt; <span class="hljs-type">SuccR</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">a</span>) b <span class="hljs-keyword">where</span></span><br>  succR = cong (succR @a @b)<br></code></pre></td></tr></table></figure><p>现在来证明交换律: <span class="math inline">\(\forall (a, b \in\mathbb{N}), a + b = b + a\)</span>, 翻译成类型</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">Comm</span> (<span class="hljs-title">a</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">b</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  comm :: (a :+: b) :~: (b :+: a)<br></code></pre></td></tr></table></figure><p>对 <code>b</code> 做归纳</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Idr</span> a =&gt; <span class="hljs-type">Comm</span> a &#x27;<span class="hljs-type">Z</span> <span class="hljs-keyword">where</span></span><br>  comm = idr<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> (<span class="hljs-type">SuccR</span> <span class="hljs-title">a</span> <span class="hljs-title">b</span>, <span class="hljs-type">Comm</span> <span class="hljs-title">a</span> <span class="hljs-title">b</span>) =&gt; <span class="hljs-type">Comm</span> a (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">b</span>) <span class="hljs-keyword">where</span></span><br>  comm = <span class="hljs-keyword">let</span> eq1 = succR @a @b <br>             eq2 = cong (comm @a @b) <br>         <span class="hljs-keyword">in</span> trans eq1 eq2 <br></code></pre></td></tr></table></figure><p>先看 <code>b</code> 为 <code>'Z</code> 的情况,此时Haskell能直接把目标类型化简为 <code>a :+: 'Z = a</code>,这就是我们的第一个引理<code>idr</code>, 直接填上就能证明.</p><p>比较复杂的是 <code>b</code> 为 <code>'S b</code> 的情况, 我们把目标<code>(a :+: 'S b) :~: 'S (b :+: a)</code> 分成两个式子:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haskell">(a :+: &#x27;<span class="hljs-type">S</span> b) :~: &#x27;<span class="hljs-type">S</span> (b :+: a)<br>分成<br><span class="hljs-title">eq1</span> :: (a :+: &#x27;<span class="hljs-type">S</span> b) :~: &#x27;<span class="hljs-type">S</span> (a :+: b)<br><span class="hljs-title">eq2</span> :: &#x27;<span class="hljs-type">S</span> (a :+: b) :~: &#x27;<span class="hljs-type">S</span> (b :+: a)<br></code></pre></td></tr></table></figure><p>第一个式子就是我们证明的第二个引理 <code>succR</code>,所以我们在约束上写<code>SuccR a b</code>; 第二个式子两边都去掉<code>'S</code> 就是 <code>comm @a @b</code>,所以我们在约束上写<code>Comm a b</code>,然后再在<code>comm @a @b</code>类型的等式两边同时用<code>cong</code>加上<code>'S</code>函数,就能得到 <code>eq2</code>.</p><p>最后再用等式的传递性连接 <code>eq1</code> 和 <code>eq2</code>就能得到最终的结果:</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-title">trans</span> eq1 eq2 :: (a :+: &#x27;<span class="hljs-type">S</span> b) :~: &#x27;<span class="hljs-type">S</span> (b :+: a)<br></code></pre></td></tr></table></figure><h2 id="完整代码">完整代码</h2><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br></pre></td><td class="code"><pre><code class="hljs haskell"><span class="hljs-meta">&#123;-# LANGUAGE MultiParamTypeClasses #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE FlexibleInstances  #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE TypeApplications #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE DataKinds #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE TypeFamilies #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE TypeOperators #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE KindSignatures #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE GADTs #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE UndecidableInstances #-&#125;</span><br><span class="hljs-meta">&#123;-# LANGUAGE AllowAmbiguousTypes #-&#125;</span><br><span class="hljs-meta">&#123;-# OPTIONS_GHC -Wno-missing-export-lists #-&#125;</span><br><br><span class="hljs-keyword">module</span> Test <span class="hljs-keyword">where</span><br><br><span class="hljs-keyword">import</span> Data.Kind (<span class="hljs-type">Type</span>)<br><br><span class="hljs-keyword">infixr</span> <span class="hljs-number">4</span> :+:<br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">HList</span> :: [<span class="hljs-type">Type</span>] -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">HNil</span>  :: <span class="hljs-type">HList</span> &#x27;[]<br>  (:+:) :: t -&gt; <span class="hljs-type">HList</span> ts -&gt; <span class="hljs-type">HList</span> (t &#x27;: ts)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Show</span> (<span class="hljs-type">HList</span> &#x27;[]) <span class="hljs-keyword">where</span></span><br>  show <span class="hljs-type">HNil</span> = <span class="hljs-string">&quot;HNil&quot;</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> (<span class="hljs-type">Show</span> <span class="hljs-title">t</span>, <span class="hljs-type">Show</span> (<span class="hljs-type">HList</span> <span class="hljs-title">ts</span>)) =&gt; <span class="hljs-type">Show</span> (<span class="hljs-type">HList</span> (<span class="hljs-title">t</span> &#x27;: <span class="hljs-title">ts</span>)) <span class="hljs-keyword">where</span></span><br>  show (x :+: xs) = show x ++ <span class="hljs-string">&quot;:+:&quot;</span> ++ show xs<br><br><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> <span class="hljs-type">TAppend</span> (<span class="hljs-title">ls1</span> :: [<span class="hljs-type">Type</span>]) (<span class="hljs-title">ls2</span> :: [<span class="hljs-type">Type</span>]) :: [<span class="hljs-type">Type</span>] where</span><br>  <span class="hljs-type">TAppend</span> &#x27;[] ys = ys<br>  <span class="hljs-type">TAppend</span> (x &#x27;: xs) ys = x &#x27;: <span class="hljs-type">TAppend</span> xs ys<br><br><span class="hljs-title">hAppend</span> :: <span class="hljs-type">HList</span> ts1 -&gt; <span class="hljs-type">HList</span> ts2 -&gt; <span class="hljs-type">HList</span> (<span class="hljs-type">TAppend</span> ts1 ts2)<br><span class="hljs-title">hAppend</span> <span class="hljs-type">HNil</span> ys = ys<br><span class="hljs-title">hAppend</span> (x :+: xs) ys = x :+: hAppend xs ys<br><br><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> <span class="hljs-type">Map</span> (<span class="hljs-title">f</span> :: <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span>) (<span class="hljs-title">ts</span> :: [<span class="hljs-type">Type</span>]) :: [<span class="hljs-type">Type</span>] where</span><br>  <span class="hljs-type">Map</span> f &#x27;[] = &#x27;[]<br>  <span class="hljs-type">Map</span> f (t &#x27;: ts) = f t &#x27;: <span class="hljs-type">Map</span> f ts<br><br><span class="hljs-comment">-- hSequence :: forall (m :: Type -&gt; Type) (ts :: [Type]) . Monad m =&gt; HList (Map m ts) -&gt; m (HList ts)</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">HSeq</span> (<span class="hljs-title">ts</span> :: [<span class="hljs-type">Type</span>]) <span class="hljs-keyword">where</span></span><br>  hSequence :: <span class="hljs-type">Monad</span> m =&gt; <span class="hljs-type">HList</span> (<span class="hljs-type">Map</span> m ts) -&gt; m (<span class="hljs-type">HList</span> ts)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">HSeq</span> &#x27;[] <span class="hljs-keyword">where</span></span><br>  hSequence <span class="hljs-type">HNil</span> = return <span class="hljs-type">HNil</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">HSeq</span> ts =&gt; <span class="hljs-type">HSeq</span> (<span class="hljs-title">t</span> &#x27;: <span class="hljs-title">ts</span>) <span class="hljs-keyword">where</span></span><br>  hSequence (x :+: xs) = <span class="hljs-keyword">do</span><br>    xr &lt;- x<br>    xsr &lt;- hSequence xs<br>    return (xr :+: xsr)<br><br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Nat</span> = <span class="hljs-type">Z</span> | <span class="hljs-type">S</span> <span class="hljs-type">Nat</span></span><br><br><span class="hljs-keyword">infixr</span> <span class="hljs-number">4</span> :::<br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Vect</span> :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">VNil</span> :: <span class="hljs-type">Vect</span> &#x27;<span class="hljs-type">Z</span> a<br>  (:::) :: a -&gt; <span class="hljs-type">Vect</span> n a -&gt; <span class="hljs-type">Vect</span> (&#x27;<span class="hljs-type">S</span> n) a<br><br><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> (:+) (<span class="hljs-title">m</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) :: <span class="hljs-type">Nat</span> where</span><br>  &#x27;<span class="hljs-type">Z</span>     :+ n = n<br>  (&#x27;<span class="hljs-type">S</span> m) :+ n = &#x27;<span class="hljs-type">S</span> (m :+ n)<br><br><span class="hljs-title">vAppend</span> :: <span class="hljs-type">Vect</span> m a -&gt; <span class="hljs-type">Vect</span> n a -&gt; <span class="hljs-type">Vect</span> (m :+ n) a<br><span class="hljs-title">vAppend</span> <span class="hljs-type">VNil</span> ys = ys<br><span class="hljs-title">vAppend</span> (x:::xs) ys = x ::: vAppend xs ys<br><br><br><br><span class="hljs-title">nat2Int</span> :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Int</span><br><span class="hljs-title">nat2Int</span> <span class="hljs-type">Z</span> = <span class="hljs-number">0</span><br><span class="hljs-title">nat2Int</span> (<span class="hljs-type">S</span> n) = <span class="hljs-number">1</span> + nat2Int n<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Show</span> <span class="hljs-type">Nat</span> <span class="hljs-keyword">where</span></span><br>  show = (<span class="hljs-string">&quot;n&quot;</span>++) . show . nat2Int<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Num</span> <span class="hljs-type">Nat</span> <span class="hljs-keyword">where</span></span><br>  <span class="hljs-type">Z</span>     + n = n<br>  (<span class="hljs-type">S</span> m) + n = <span class="hljs-type">S</span> (m + n)<br><br>  <span class="hljs-type">Z</span>     - _     = <span class="hljs-type">Z</span><br>  m     - <span class="hljs-type">Z</span>     = m<br>  (<span class="hljs-type">S</span> m) - (<span class="hljs-type">S</span> n) = m - n<br><br>  <span class="hljs-type">Z</span>     * _ = <span class="hljs-type">Z</span><br>  (<span class="hljs-type">S</span> m) * n = n + (m * n)<br><br>  fromInteger <span class="hljs-number">0</span> = <span class="hljs-type">Z</span><br>  fromInteger n = <span class="hljs-type">S</span> $ fromInteger (n - <span class="hljs-number">1</span>)<br><br>  abs x = x<br>  signum _ = <span class="hljs-number">1</span><br><br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">SNat</span> :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">SZ</span> :: <span class="hljs-type">SNat</span> &#x27;<span class="hljs-type">Z</span><br>  <span class="hljs-type">SS</span> :: <span class="hljs-type">SNat</span> n -&gt; <span class="hljs-type">SNat</span> (&#x27;<span class="hljs-type">S</span> n)<br><br><span class="hljs-keyword">infixl</span> <span class="hljs-number">6</span> |+|<br>(|+|) :: <span class="hljs-type">SNat</span> m -&gt; <span class="hljs-type">SNat</span> n -&gt; <span class="hljs-type">SNat</span> (m :+: n)<br><span class="hljs-type">SZ</span>   |+| n = n<br><span class="hljs-type">SS</span> m |+| n = <span class="hljs-type">SS</span> (m |+| n)<br><br><span class="hljs-keyword">infixl</span> <span class="hljs-number">7</span> |*|<br>(|*|) :: <span class="hljs-type">SNat</span> m -&gt; <span class="hljs-type">SNat</span> n -&gt; <span class="hljs-type">SNat</span> (m :*: n)<br><span class="hljs-type">SZ</span>   |*| _ = <span class="hljs-type">SZ</span><br><span class="hljs-type">SS</span> m |*| n = n |+| (m |*| n)<br><br><br><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> (:+:) (<span class="hljs-title">m</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) :: <span class="hljs-type">Nat</span> where</span><br>  &#x27;<span class="hljs-type">Z</span>     :+: n = n<br>  (&#x27;<span class="hljs-type">S</span> m) :+: n = &#x27;<span class="hljs-type">S</span> (m :+: n)<br><br><span class="hljs-class"><span class="hljs-keyword">type</span> <span class="hljs-keyword">family</span> (:*:) (<span class="hljs-title">m</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) :: <span class="hljs-type">Nat</span> where</span><br>  &#x27;<span class="hljs-type">Z</span>     :*: _ = &#x27;<span class="hljs-type">Z</span><br>  (&#x27;<span class="hljs-type">S</span> m) :*: n = n :+: (m :*: n)<br><br><br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Proxy</span> t = <span class="hljs-type">Proxy</span></span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">FromNat</span> (<span class="hljs-title">nat</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  fromNat :: <span class="hljs-type">Proxy</span> nat -&gt; <span class="hljs-type">Nat</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">FromNat</span> &#x27;<span class="hljs-type">Z</span> <span class="hljs-keyword">where</span></span><br>  fromNat _ = <span class="hljs-number">0</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">FromNat</span> n =&gt; <span class="hljs-type">FromNat</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">n</span>) <span class="hljs-keyword">where</span></span><br>  fromNat _ = <span class="hljs-type">S</span> $ fromNat (<span class="hljs-type">Proxy</span> :: <span class="hljs-type">Proxy</span> n)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">ToSNat</span> (<span class="hljs-title">nat</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  toSNat :: <span class="hljs-type">Proxy</span> nat -&gt; <span class="hljs-type">SNat</span> nat<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">ToSNat</span> &#x27;<span class="hljs-type">Z</span> <span class="hljs-keyword">where</span></span><br>  toSNat <span class="hljs-type">Proxy</span> = <span class="hljs-type">SZ</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">ToSNat</span> n =&gt; <span class="hljs-type">ToSNat</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">n</span>) <span class="hljs-keyword">where</span></span><br>  toSNat <span class="hljs-type">Proxy</span> = <span class="hljs-type">SS</span> (toSNat (<span class="hljs-type">Proxy</span> :: <span class="hljs-type">Proxy</span> n))<br><br><span class="hljs-title">fromList</span> :: <span class="hljs-type">SNat</span> n -&gt; [a] -&gt; <span class="hljs-type">Vect</span> n a<br><span class="hljs-title">fromList</span> <span class="hljs-type">SZ</span>     _        = <span class="hljs-type">VNil</span><br><span class="hljs-title">fromList</span> (<span class="hljs-type">SS</span> n) (x : xs) = x ::: fromList n xs<br><span class="hljs-title">fromList</span> _ _ = error <span class="hljs-string">&quot;Index out of range&quot;</span><br><br><span class="hljs-class"><span class="hljs-keyword">data</span> <span class="hljs-type">Fin</span> :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">FZ</span> :: <span class="hljs-type">Fin</span> (&#x27;<span class="hljs-type">S</span> k)<br>  <span class="hljs-type">FS</span> :: <span class="hljs-type">Fin</span> k -&gt; <span class="hljs-type">Fin</span> (&#x27;<span class="hljs-type">S</span> k)<br><br><span class="hljs-keyword">infixl</span> <span class="hljs-number">9</span> !<br>(!) :: <span class="hljs-type">Vect</span> n a -&gt; <span class="hljs-type">Fin</span> n -&gt; a<br>(x ::: _)  ! <span class="hljs-type">FZ</span>     = x<br>(_ ::: xs) ! (<span class="hljs-type">FS</span> f) = xs ! f<br><br><br><span class="hljs-class"><span class="hljs-keyword">data</span> (:&lt;:) :: <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Nat</span> -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">LZ</span> :: &#x27;<span class="hljs-type">Z</span> :&lt;: &#x27;<span class="hljs-type">S</span> n<br>  <span class="hljs-type">LS</span> :: m :&lt;: n -&gt; &#x27;<span class="hljs-type">S</span> m :&lt;: &#x27;<span class="hljs-type">S</span> n<br><br><span class="hljs-title">twoLtfour</span> :: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>) :&lt;: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)))<br><span class="hljs-title">twoLtfour</span> = <span class="hljs-type">LS</span> (<span class="hljs-type">LS</span> (<span class="hljs-type">LZ</span> :: &#x27;<span class="hljs-type">Z</span> :&lt;: &#x27;<span class="hljs-type">S</span> (&#x27;<span class="hljs-type">S</span> &#x27;<span class="hljs-type">Z</span>)))<br><br><br><span class="hljs-keyword">infixl</span> <span class="hljs-number">4</span> :~:<br><span class="hljs-class"><span class="hljs-keyword">data</span> (:~:) :: k -&gt; k -&gt; <span class="hljs-type">Type</span> where</span><br>  <span class="hljs-type">Refl</span> :: a :~: a<br><br><span class="hljs-title">cong</span> :: a :~: b -&gt; f a :~: f b<br><span class="hljs-title">cong</span> <span class="hljs-type">Refl</span> = <span class="hljs-type">Refl</span><br><br><span class="hljs-title">sym</span> :: a :~: b -&gt; b :~: a<br><span class="hljs-title">sym</span> <span class="hljs-type">Refl</span> = <span class="hljs-type">Refl</span><br><br><span class="hljs-title">trans</span> :: a :~: b -&gt; b :~: c -&gt; a :~: c<br><span class="hljs-title">trans</span> <span class="hljs-type">Refl</span> <span class="hljs-type">Refl</span> = <span class="hljs-type">Refl</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">Assoc</span> (<span class="hljs-title">a</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">b</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">c</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  assoc :: ((a :+: b) :+: c) :~: (a :+: (b :+: c))<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Assoc</span> &#x27;<span class="hljs-type">Z</span> b c <span class="hljs-keyword">where</span></span><br>  assoc = <span class="hljs-type">Refl</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Assoc</span> a b c =&gt; <span class="hljs-type">Assoc</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">a</span>) b c <span class="hljs-keyword">where</span></span><br>  assoc = cong @_ @_ @&#x27;<span class="hljs-type">S</span> (assoc @a @b @c)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">Idr</span> (<span class="hljs-title">n</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  idr :: (n :+: &#x27;<span class="hljs-type">Z</span>) :~: n<br><span class="hljs-class">  </span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Idr</span> &#x27;<span class="hljs-type">Z</span> <span class="hljs-keyword">where</span></span><br>  idr = <span class="hljs-type">Refl</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Idr</span> n =&gt; <span class="hljs-type">Idr</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">n</span>) <span class="hljs-keyword">where</span></span><br>  idr = cong @(n:+:&#x27;<span class="hljs-type">Z</span>) @n @&#x27;<span class="hljs-type">S</span> (idr @n)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">SuccR</span> (<span class="hljs-title">a</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">b</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  succR :: (a :+: &#x27;<span class="hljs-type">S</span> b) :~: &#x27;<span class="hljs-type">S</span> (a :+: b)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">SuccR</span> &#x27;<span class="hljs-type">Z</span> b <span class="hljs-keyword">where</span></span><br>  succR = <span class="hljs-type">Refl</span><br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">SuccR</span> a b =&gt; <span class="hljs-type">SuccR</span> (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">a</span>) b <span class="hljs-keyword">where</span></span><br>  succR = cong (succR @a @b)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-type">Comm</span> (<span class="hljs-title">a</span> :: <span class="hljs-type">Nat</span>) (<span class="hljs-title">b</span> :: <span class="hljs-type">Nat</span>) <span class="hljs-keyword">where</span></span><br>  comm :: (a :+: b) :~: (b :+: a)<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> <span class="hljs-type">Idr</span> a =&gt; <span class="hljs-type">Comm</span> a &#x27;<span class="hljs-type">Z</span> <span class="hljs-keyword">where</span></span><br>  comm = idr<br><span class="hljs-class"></span><br><span class="hljs-class"><span class="hljs-keyword">instance</span> (<span class="hljs-type">SuccR</span> <span class="hljs-title">a</span> <span class="hljs-title">b</span>, <span class="hljs-type">Comm</span> <span class="hljs-title">a</span> <span class="hljs-title">b</span>) =&gt; <span class="hljs-type">Comm</span> a (&#x27;<span class="hljs-type">S</span> <span class="hljs-title">b</span>) <span class="hljs-keyword">where</span></span><br>  comm = <span class="hljs-keyword">let</span> eq1 = succR @a @b <br>             eq2 = cong (comm @a @b) <br>         <span class="hljs-keyword">in</span> trans eq1 eq2 <br></code></pre></td></tr></table></figure>]]></content>
    
    
    
    <tags>
      
      <tag>Haskell</tag>
      
      <tag>编程</tag>
      
      <tag>计算机</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>热力学笔记</title>
    <link href="/2023/09/20/%E7%83%AD%E5%8A%9B%E5%AD%A6%E7%AC%94%E8%AE%B0/"/>
    <url>/2023/09/20/%E7%83%AD%E5%8A%9B%E5%AD%A6%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<p>写着呢😭 <!-- <div class="row">    <embed src="https://konjacsource.github.io/pdfs/热力学笔记.pdf" width="100%" height="1500px" type="application/pdf"></div> --&gt;</p>]]></content>
    
    
    
    <tags>
      
      <tag>微分几何</tag>
      
      <tag>热学</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>二次量子化笔记</title>
    <link href="/2023/08/19/%E4%BA%8C%E6%AC%A1%E9%87%8F%E5%AD%90%E5%8C%96%E7%AC%94%E8%AE%B0/"/>
    <url>/2023/08/19/%E4%BA%8C%E6%AC%A1%E9%87%8F%E5%AD%90%E5%8C%96%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<div class="row">    <embed src="https://konjacsource.github.io/pdfs/%E4%BA%8C%E6%AC%A1%E9%87%8F%E5%AD%90%E5%8C%96.pdf" width="100%" height="1500px" type="application/pdf"></div>]]></content>
    
    
    
    <tags>
      
      <tag>量子力学</tag>
      
      <tag>量子场论</tag>
      
      <tag>二次量子化</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>量子力学</title>
    <link href="/2023/06/11/%E9%87%8F%E5%AD%90%E5%8A%9B%E5%AD%A6/"/>
    <url>/2023/06/11/%E9%87%8F%E5%AD%90%E5%8A%9B%E5%AD%A6/</url>
    
    <content type="html"><![CDATA[<div class="row">    <embed src="https://konjacsource.github.io/pdfs/量子力学.pdf" width="100%" height="1500px" type="application/pdf"></div>]]></content>
    
    
    
    <tags>
      
      <tag>量子力学</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>理论力学笔记</title>
    <link href="/2023/06/03/%E7%90%86%E8%AE%BA%E5%8A%9B%E5%AD%A6%E7%AC%94%E8%AE%B0/"/>
    <url>/2023/06/03/%E7%90%86%E8%AE%BA%E5%8A%9B%E5%AD%A6%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<div class="row">    <embed src="https://konjacsource.github.io/pdfs/%E7%90%86%E8%AE%BA%E5%8A%9B%E5%AD%A6.pdf" width="100%" height="1500px" type="application/pdf"></div>]]></content>
    
    
    
    <tags>
      
      <tag>微分几何</tag>
      
      <tag>力学</tag>
      
      <tag>理论力学</tag>
      
      <tag>辛几何</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>切丛</title>
    <link href="/2023/04/15/%E5%88%87%E4%B8%9B/"/>
    <url>/2023/04/15/%E5%88%87%E4%B8%9B/</url>
    
    <content type="html"><![CDATA[<div class="row">    <embed src="https://konjacsource.github.io/pdfs/切丛.pdf" width="100%" height="1500px" type="application/pdf"></div>]]></content>
    
    
    
    <tags>
      
      <tag>数学</tag>
      
      <tag>微分几何</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>度规的正交归一基底与体元</title>
    <link href="/2023/02/27/%E5%BA%A6%E8%A7%84%E7%9A%84%E6%AD%A3%E4%BA%A4%E5%BD%92%E4%B8%80%E5%9F%BA%E5%BA%95%E4%B8%8E%E4%BD%93%E5%85%83/"/>
    <url>/2023/02/27/%E5%BA%A6%E8%A7%84%E7%9A%84%E6%AD%A3%E4%BA%A4%E5%BD%92%E4%B8%80%E5%9F%BA%E5%BA%95%E4%B8%8E%E4%BD%93%E5%85%83/</url>
    
    <content type="html"><![CDATA[<div class="row">    <embed src="https://konjacsource.github.io/pdfs/度规的正交归一基底与体元.pdf" width="100%" height="1500px" type="application/pdf"></div>]]></content>
    
    
    
    <tags>
      
      <tag>数学</tag>
      
      <tag>微分几何</tag>
      
      <tag>广义相对论</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
